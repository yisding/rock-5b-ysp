# Reproducer build artifacts never live in git; sources are *.c / *.frag /
# *.vert / *.md. Compiled shaders and the extensionless probe executables are
# generated by the build commands in README.md / README-arm-blob.md.
*.spv

# Compiled probe executables (basename == source minus .c; none of the tracked
# sources are extensionless, so these patterns never match a source file).
probe_interp
probe_interp_explained
tiny_interp_probe
tiny_interp_probe_explained
tiny_interp_probe_arm_blob
tiny_interp_probe_arm_blob_explained
tiny_interp_probe_arm_blob_x11
tiny_interp_probe_arm_blob_x11_explained
tex_interp_probe
exact_offset_scan
exact_offset_scan2d
offset_perf_probe
offset_perf_verify
triangle_matrix_probe
vk_interp_probe
vk_interp_probe_explained
vk_interp_probe_arm_blob
vk_interp_probe_arm_blob_explained
