# ffmpeg-rockchip-81 package validation failures and MPP root cause

promoted → [`../video-libraries/ffmpeg/docs/rockchip81-package-validation.md`](../video-libraries/ffmpeg/docs/rockchip81-package-validation.md) (2026-07-08)

The package build + on-board smoke validation (the passes, the four documented
failures, and the incomplete-`/usr`-MPP decode root cause) now live in that doc.
