# findings/ — raw capture inbox

Low-ceremony landing zone for a technical fact you or an agent just learned while
reading code in one of the `../` source trees. **Drop first, sort later.** The bar
to add a file here is deliberately low: one fact, dated, with where it came from.

This is the write path that the polished per-project `docs/` do **not** offer —
depositing into a package doc means editing an index table and matching house
style, so hard-won detail gets re-derived instead of written down. Here you just
add a file.

## How to deposit

1. Copy [`TEMPLATE.md`](TEMPLATE.md) to `YYYY-MM-DD-<short-slug>.md`.
2. Fill the header (scope, source anchor, date, trust tag) and write the fact.
3. Add one line to the index below. That's it — no other file needs editing.

For promotion, status changes, and the repository handoff checklist, follow the
canonical [`CONTRIBUTING.md`](../CONTRIBUTING.md) workflow.

Trust tags state what kind of evidence supports each claim. Combine tags when a
finding mixes evidence types:

- **MEASURED** — observed on hardware or in a recorded run.
- **CODE-INSPECTED**, **CONFIG-INSPECTED**, or **SOURCE-INSPECTED** — checked
  directly against the named pinned source.
- **COMPILE-VERIFIED** — the affected source compiled, but runtime behavior was
  not exercised.
- **CONFIRMED** — independent evidence corroborates the stated attribution.
- **INFERRED** — a conclusion supported by the recorded evidence but not
  observed directly.
- **HYPOTHESIS** — a candidate explanation that still needs a discriminating
  test.
- **DESIGN** — proposed behavior or an implementation plan, not an observed
  result.
- **UNVERIFIED** — copied from a comment, commit, or other source but not yet
  checked.

## Lifecycle

A finding is raw by default. When it matures into durable reference, **graduate**
it: move the content into the owning project's `docs/`, and replace the file here
with a one-line tombstone (`promoted → <path> (YYYY-MM-DD)`) so the trail survives.
Findings that turn out wrong get deleted with a one-line note in the index.

**Boundary vs [`status.md`](../status.md) watchlist:** the watchlist tracks
*facts that go stale silently* (external PRs, distro versions, dev-box SPOFs).
`findings/` holds *newly-learned technical detail*. A finding with a follow-up
action belongs here; a stale-risk to re-check on every maintenance pass belongs
in the watchlist.

## Index (newest first)

Each row: `` `YYYY-MM-DD-slug.md` `` — one-line summary — trust tag.

- `` `2026-07-24-rock5b-spi-vs-radxa-bsp-firmware-generation-gap.md` `` — **the "SPI U-Boot can't boot Radxa BSP images" theory is falsified for the current Debian BSP (rsdk r6).** Live `/dev/mtdblock0` re-verified 2026-07-24 as still `38b40ad1…` (= repo `spi-rock5b-20260706.bin` = Armbian `rkspi_loader.img`, built 2026-06-05). Against r6 the installed SPI U-Boot is the *same* U-Boot for everything governing OS discovery: same vendor commit (`S39cd` vs `rk2410-2017.09-64-39cd993-g58d424f` — both `radxa/u-boot` `39cd993`), **byte-identical 51-variable default environment** (incl. `bootcmd`, `boot_targets`, `scan_dev_for_boot_part`'s `boot_android`, `fdtfile`), **functionally identical 12,752-B control DTB** (28-line diff = pure reordering of `crypto@fe370000`/`rng@fe378000`), and the same SPI layout Radxa's own `setup.sh:build_spinor` produces (idbloader seek 64, `u-boot.itb` seek 16384). The earlier `boot_android`-placement divergence is against the **2024** Radxa SPI image, not current BSP. The real delta is a **firmware blob generation gap**: DDR v1.20 `b8ce94f14b` vs **v1.22 `d4bf75a5a6`**, and BL31 **v1.48 (204,860 B @ `0x40000`) vs v1.54 (149,020 B @ `0x60000`)** — base moved and shrank 55,840 B. Mechanism: with SPI populated, BootROM runs the SPI chain, so the BSP image's own idbloader/`u-boot.itb` (verified byte-identical to `/usr/lib/u-boot/rock-5b/`) never execute and the BSP OS runs on firmware it wasn't built against. Ruled out with evidence: extlinux/`fdtdir` resolution (BSP DTB present at the expected path), ext4 features (our booting rootfs uses a *superset*), UEFI (the "efi" partition is empty), bootable-attribute selection, and saved env (all-zero `uboot_env`). Untested-boundary: **no image was written and no boot attempted** — whether BL31 v1.48 actually blocks a BSP boot vs degrading SIP-dependent features is unproven (no hard version gate in the r6 kernel). Flags a likely false-negative symptom: `console=ttyFIQ0` needs a BL31 share-memory SIP call, so UART can go silent while the kernel continues on `tty1` — MEASURED / CODE-INSPECTED / INFERRED / HYPOTHESIS (ttyFIQ0 silence).
- `` `2026-07-24-mali-blit-workaround-size-results.md` `` — concise, shareable MR !43161 summary plus focused wrapper reproducer: canonical bit-exact baseline-vs-zero-offset equality was observed only when both destination dimensions are powers of two; integer-bin fullscreen-triangle scans pass through `1x1..1024x1024`, the default `1x1..1500x1500` run finds only `1x1480`/`1x1490`, the `1x1..2080x2080` run finds 85 failures all accounted for by line/near-line cases (`2080x1`, sparse `1xH`, `2x2080`), and canonical `25..50` plus `50..100` aspect bands through `16Kx16K` find 0 failures; this does not prove ordinary non-integer TEX safety; `16383x127` is the lowest measured full-matrix failing aspect (`129.000`) but the canonical orientation passes wide and fails tall — MEASURED / INFERRED.
- `` `2026-07-24-mali-oblong-triangle-matrix.md` `` — full MR !43161 technical record: separates bit-exact equality, integer/TXF-style correctness, and ordinary non-integer `texture()` correctness; records the 256-case topology matrix, canonical `1x1..2080x2080` scan, canonical `25..50` and `50..100` aspect-band scans through `16Kx16K`, line scans, power-of-two exactness controls through `4096x4096`, aspect-ratio failures down to `16383x127` (`129.000`), jagged passing neighbors, and the Mesa-path conclusion that the robust predicate is the affected Panfrost fullscreen blitter path (`arch >= 9 && arch < 11`), with size/aspect gating only as an engineering compromise — MEASURED / CONFIRMED / INFERRED.
- `` `2026-07-24-rknpu-forward-port-scoping.md` `` — scopes forward-porting the BSP `drivers/rknpu` (0.9.8) onto the ysp 6.18 kernel: **8,598 lines / 12 files — about a fifth of the already-ported MPP+RGA code** — mostly conventional, with three concentrated hard spots: `rknpu_iommu.c` casts `domain->iova_cookie` to a private `iommu_dma_cookie` mirror + multi-domain switching (dead on 6.18; needs the `0003`/`0006`/`0013`-style owned-domain rewrite), `rknpu_devfreq.c` depends on BSP `soc/rockchip` OPP/system-monitor/IPA infra absent from the 6.18 tree (but stubs out cleanly under `CONFIG_PM_DEVFREQ` with a fixed clock), and mainline's new `drivers/accel/rocket` + per-core `rknn_core_*` DT nodes claim the same silicon (rocket is librknnrt-incompatible — disable + BSP-style DT node, an explicit vendor-ABI-over-mainline decision). Mechanical rest: `rknpu_gem.c` pfn_t removal; drop DMA_HEAP, skip SRAM initially. Estimate: 2–4 days to probing, 1–2 weeks to validated inference (C16 evidence) — SOURCE-INSPECTED / INFERRED (estimates).
- `` `2026-07-23-forward-port-current-tip-full-validation-run.md` `` — full validation ladder against the **latest forward-port kernel** (KASAN+lockdep `#8`, `av1-fwport@4401383a6d9b5`, tail `0001`–`0072`; notes `94b76691`, vmlinuz md5 `6bb59e4a`, built 07:39; no `P####` recorded). **All correctness + memory-safety rungs green**: decode bit-exact (PSNR inf) for H.264/H.265/VP9/AV1; ABI probe/replay pass; librga base smoke green (10-bit skipped — staged librga isn't the P010 fork); KASAN 12/12 MPP matrix `flagged=0`, narrowed reset-session repro, `0070` re-init→`EBUSY`, `0058` clientless RELEASE_FD survives, cross-session UAF 64,000 submits `flagged=0`; whole-boot fatal sweep 0. **`0072` runtime-verified present**: the scattered-userptr fuzzer now fails closed (driver rejects a non-16-aligned IOMMU base) instead of silent zero output; fuzz oracle corrected to expect the reject (all modes `FAIL=0`), and `run-root-gates.sh` VP9 gate un-gated. FFmpeg suite 24 cases with **one userspace defect**: the harness's default `FFDIR` binary (FFmpeg-**master**, `libavcodec 63`) **deadlocks** on `h264→hevc` (required) and `hevc_main10→p010` (diagnostic) `rkmpp` pipelines — all threads on `futex`, **kernel not implicated**; the shipping `/usr/bin/ffmpeg 8.0.3~rk1` (`libavcodec 62`, carries `da5befc806`) runs both cleanly, so it's a build lacking our backpressure fix, not the kernel (already-catalogued submission-plan §B hang class, not new). Root-only gates (encoder/transcode/`run-root-gates`) **blocked on `sudo` password**. KASAN build ⇒ no perf/soak claims — MEASURED.
- `` `2026-07-23-rock5b-boot-hang-recurred-with-patched-plymouth.md` `` — the intermittent boot stall **recurred at 09:56 with the patched Plymouth provably in the boot path**: `~rk1` packages installed 05:35, initramfs+uInitrd regenerated 05:41, the hung boot's uInitrd payload byte-identical to the installed files, and disassembly of the stripped `libply-splash-core` confirms the upstream `45655f12` `continue`→`break` fix is in the running binary (stock lib branches back to the outer loop head without advancing; patched lib exits via `ply_buffer_remove_bytes`). Identical fingerprint (read-write/show-splash stuck, no `SIGRTMIN+20`, no `sysinit.target`) ⇒ the incomplete-CSI parser loop is **falsified as the sole internal wedge**; boot-transaction analysis stands, internal cause back to unknown. `O_NONBLOCK` terminal fds also exclude a blocked serial `write()`. Next discriminator: `debug-shell.service` + `plymouth.debug=stream:/dev/ttyS2`, capture the live `plymouthd` stack instead of resetting; `plymouth.enable=0` (still unapplied) remains the reliable mitigation — MEASURED / CODE-INSPECTED / prior attribution FALSIFIED-AS-SOLE-CAUSE.
- `` `2026-07-24-rga3-legacy-blit-10bit-stride-convention-fault.md` `` — the GStreamer NV12_10 RGA IOMMU read faults from the production conformance run are **a kernel regression from our own `0048`** (`4b2beb91521f1`): the legacy `RGA_BLIT` ABI passes 10-bit `vir_w` as a **byte** stride (BSP contract, preserved by our RGA2 leg), but since `0048` the **RGA3** writer treats it as **pixels** and programs `ALIGN(vir_w*10/8,16)` = 560-byte rows for a 448-byte surface → reads 201600 B from a 161280–193536 B buffer → IOMMU read fault, `EACCES`; the scheduler prefers RGA3, so every legacy 10-bit blit fails. **Proven by forced-core discriminating repro** (below-4G CMA dma-bufs, identical request): RGA2 `ret=0`, RGA3 faults; unforced → RGA3. Pre-`0048` BSP programmed 1 byte/px — accidentally right for legacy, corrupt for im2d P010/NV15 (what `0048` fixed); both conventions share `vir_w` *and* `RGA_BLIT_SYNC`, so no entry-point disambiguation. **Fix IMPLEMENTED same day**: kernel `0072` (`138f0de2c972`, RGA3 byte-literal 10-bit strides + align-check byte semantics) and `0073` (`79fc616390e5`, RGA2 MMU page-table fails closed `-EOPNOTSUPP` on >4G entries — the secondary bus-error observation) compile/checkpatch-clean and exported to the series; paired librga-fork `c80eea7` converts im2d pixel wstride→bytes in `generate_blit_req`/`generate_fill_req` (builds clean; must land with `0072`). Booted gates pending the next debug build (`rga-10bit-legacy-stride-test` all cores + P010/NV15 + GStreamer NV12_10). **Rewrite drivers aligned the same day** (`185d4dc` on 6.18 / `d5165ca` on mainline): its byte-literal register writers were fronted by pixel-math layout/validation (legacy fail-closed rejected, pixel callers under-strided — internally inconsistent, no booted 10-bit evidence existed) — byte semantics adopted end-to-end for raster with a new active-window byte-fit check, TILE/FBC keep pixels, KUnit + ABI.rst updated; no `0073` analog needed (32-bit RGA2 DMA mask, no legacy page table). JeffyCN pool geometry vs vdpu381 (`768 - 448` buf_slot mismatch) remains a plugin-side concern — MEASURED / SOURCE-CONFIRMED / FIX-COMPILE-VERIFIED.
- `` `2026-07-24-production-ppa-kernel-full-conformance-run.md` `` — the **Published PPA production (non-KASAN) kernel** `6.18.38+rk3588av1fwport20260723-0ubuntu1~rk1` (Launchpad-built, renumbered tail `0001`–`0071`) booted and passed the **full driver conformance set on-board**: ABI probe/replay, official MPP matrix **12/12**, FFmpeg suite **24/24 with AV1 required** (incl. the two cases that deadlocked the stale `FFDIR` master binary — the `ffmpeg-rockchip-81` default runs them clean), decode-differential **bit-exact H.264/H.265/VP9/AV1**, `test-decode`, `librga-smoke`, and **all root gates** (`encode`/`transcode`/`rga-mmu-debug`/`iommu-machinery-fuzz` PASS, `mpp-debug-capture` expected SKIP, **VP9 `show_existing_frame` survives clean** — first production-build survival), every kernel scan + the whole-battery journal sweep clean. Production build ⇒ perf evidence stands (H.265 720p encode ~353 fps @ 60–62.6 dB; transcode 20.8×/88× realtime). **First-ever GStreamer suite run**: 129/133 required; two failures are userspace caps/flush issues, and the **NV12_10 legacy-blit RGA IOMMU read-fault** pair was subsequently root-caused to a `0048` kernel regression (see the stride-convention finding above; the initial plugin-side suspicion was wrong). librga demo matrix failures reconfirmed as the pre-existing `/data`+missing-heap environment gap (2 cases improved). Harness gaps recorded: mpp-suite silent `missing-env` default, runner abort-on-first-suite-failure, dev-box `CLIP_DIR` default, stale VP9-gate out-dir. Remaining: rollback validation, NV12_10 triage, P010 librga fork staging — MEASURED.
- `` `2026-07-23-rga-scattered-userptr-unaligned-src-zero-output.md` `` — surfaced by the **first-ever on-hardware run of the `iommu-machinery-fuzz` root gate**: an RGA blit whose source is a **scattered (fragmented) userptr** returns **all-zero output** unless the source byte offset is a multiple of 16, silently (no error, no IOMMU fault, no KASAN). Deterministic — passes at `src_off` ∈ {0,16,32,48}, fails everywhere else with `got=00`. Not a `0071` regression; the contiguous path handles the same unaligned offsets correctly, so it's specific to the driver-owned scatter / cache-line-unaligned-VA (`shadow_page`) handling vs RGA3's 16-byte source-alignment requirement. Narrow trigger (userptr + fragmented + unaligned), but a silent wrong-output correctness bug. **Root cause confirmed:** RGA3 fetches the source from a 16-byte-aligned base, but the `shadow_page` scatter path puts the full sub-page byte offset in the base and copies data to `shadow[offset]` with a zeroed head, so a non-16-aligned offset reads the zero head; fixed by `0072` (`4401383a6d9b5`): reject a non-16-aligned IOMMU base with `-EINVAL` rather than corrupt silently (the sub-pixel residuals are unrepresentable, so full unaligned support is a deferred format-aware enhancement). **Now runtime-verified** on a booted KASAN `#8` build (`av1-fwport@4401383a6d9b5`): the driver rejects the misaligned base and the fuzz oracle was corrected to expect it. (Correction: the reject also covers *contiguous* unaligned userptr — any non-cache-aligned offset takes the same shadow-head path, so the reject set equals the corrupt set, not just the fragmented case.) — MEASURED / SOURCE-CONFIRMED / FIX-RUNTIME-VERIFIED.
- `` `2026-07-22-rga-mm-session-debugfs-uaf-freed-task-struct.md` `` — new **forward-port** RGA defect found running the root-only gates: reading `/sys/kernel/debug/rkrga/mm_session` triggers a **KASAN slab-use-after-free** acquiring `mm->lock` in `rga_mm_session_show()` and wedges the reader in **unkillable D state** (reboot required). Root cause is ours, not the BSP: forward-port commit `bc086cbe03d72c` rewrote `rga_mm_session_release_buffer()` to free buffers via `kref_put(rga_mm_kref_release_buffer)`, which **drops and re-acquires `mm->lock` mid-`idr_for_each_entry`** during teardown, leaving the lock owned by an exited/RCU-freed `task_struct`; the next `mm->lock` waiter reads the freed owner via `mutex_can_spin_on_owner`. Same commit's `buffer->session = NULL` also NULL-derefs the debugfs dumper. Fixed by `e7eaa8f8c69b4` (free the last ref under the held lock like the BSP; NULL-guard the dump) — MEASURED (KASAN) / SOURCE-CONFIRMED / COMPILE-VERIFIED (runtime gate pending rebuild+boot).
- `` `2026-07-22-rock5b-boot-hang-plymouth-initramfs-daemon.md` `` — **root-caused at the boot-transaction level:** two failed boots on kernel builds `#3` and `#6` have the same fingerprint: an initramfs-inherited `plymouthd` retains its abstract socket but does not complete the real-root handshake, while timeout-free `plymouth update-root-fs --read-write` and `show-splash` clients wait indefinitely and hold `sysinit.target`; adjacent healthy boots receive the daemon's `SIGRTMIN+20` and complete the handshake in 30–60 ms. Every forward-port MPP/AV1/rkvdec2/rkvenc2 probe completed, and the `fdba*.video-codec` markers are routine mainline Hantro JPEG instances — ROOT-CAUSED (boot transaction) / MEASURED / SOURCE-INSPECTED / OPEN (internal daemon wedge).
- `` `2026-07-22-mali-varying-depth-bias-erratum-workaround.md` `` — Mesa maintainer confirmation identifies the G610 wide non-power-of-two varying drift as a hardware erratum; zero-valued polygon offset in OpenGL and the equivalent zero-valued depth-bias pipeline state in Vulkan both map to Valhall's `depth_bias_enable` bit and make the raw varying exact at 12288/16307, while the OpenGL mode also repairs normalized-coordinate ordinary `texture()`. The exact internal hardware path remains inferred, and Mesa/PanVK internal blitters must install the safe state themselves — CONFIRMED / MEASURED / SOURCE-INSPECTED / INFERRED.
- `` `2026-07-22-bsp-high-current-tip-port.md` `` — reconciles the BSP audit's 16 HIGH reviewer rows to 13 bugs, proves two were already fixed in the evolved forward-port, and ports the remaining 11 as `0059`-`0069` around current MPP/RGA2 bounce/lifetime code. The carrying KASAN/lockdep package `Pabd5-C4ad2` was then **installed, booted, and fully exercised 2026-07-22** (fingerprint-verified): bit-exact 4-codec differential, FFmpeg 24/24, GStreamer 129/4-known-userspace, clean KASAN MPP suite, and the destructive `0059`-`0069` ladder (foreign-fd→`-EBADF`, physical-import safe `-EINVAL`, RESET_SESSION/clientless-RELEASE_FD/cross-session-UAF with 64k async submits all clean) — board stayed up; root-only gates skipped. — BOOT-VERIFIED / KASAN-CLEAN (correctness+destructive) / PARTIAL (root-only + production/perf open).
- `` `2026-07-22-bsp-bug-upstream-submission-priority.md` `` — severity triage of the patched BSP bugs for upstream submission: the unprivileged memory-corruption subset — `0055` (OOB write over a `work_struct`), `0060` (`SET_SESSION_FD` type confusion), `0070` (double-init UAF of a freed `mpp_session`), `0052`/`0057`/`0042` (UAF/double-free), `0058` (DoS) — clears the report-now bar and `0055`/`0060`/`0070` now have standalone unprivileged PoCs; venue is the Rockchip BSP + Armbian (this code is not in mainline), CVEs for the OOB/type-confusion/UAF rows. Reachability is the `video`-group device nodes (any HW-decode/GUI process) — SOURCE-INSPECTED / MEASURED (PoCs).
- `` `2026-07-22-mpp-process-request-list-add-double-add-warn.md` `` — **root-caused + escalated to UAF:** calling `MPP_CMD_INIT_CLIENT_TYPE` twice on one `/dev/mpp_service` session double-adds `session->session_link` (unguarded `mpp_session_attach_workqueue()` at `mpp_common.c:492`, from the INIT case at `:1448`), persistently corrupting `queue->session_attach` and leaking the first `session->dma`. A *later* single unprivileged INIT then reads a freed `struct mpp_session` — KASAN slab-use-after-free at the same `list_add` site (not merely a `DEBUG_LIST` WARN). Deterministic reproducer ([`tests/mpp-double-init-repro.c`](../kernel-drivers/tests/mpp-double-init-repro.c)). BSP-identical unguarded code, untouched by `0059`-`0069`; fixed by `-EBUSY` re-init guard committed as `0070` (building) — BOARD-REPRODUCED / SOURCE-CONFIRMED / KASAN-UAF / FIX-COMMITTED.
- `` `2026-07-22-gstreamer-suite-forward-port-userspace-gaps.md` `` — GStreamer conformance on forward-port `Pd222-C4ad2` (JeffyCN `gstreamer-rockchip` MPP/RGA plugin): 98/102 required pass with a completely clean kernel journal (zero KASAN/BUG/Oops/iommu-fault); the four failures are userspace/environment gaps, not kernel regressions — MEASURED.
- `` `2026-07-21-forward-port-lifetime-resource-ownership-audit.md` `` — MPP/RGA ownership audit of the `0054` forward-port tail: three newly identified high-confidence UAF paths (global RGA release consuming an in-flight job ref, concurrent MPP result waiters sharing one raw task, and embedded timeout work outliving its task), two forward-port cleanup leaks (`0040`, `0053`/`0054`), a broader resource/error-unwind ledger, and direct BSP-vs-port attribution; most defects are inherited BSP code, while the local exceptions are named explicitly — CODE-INSPECTED / SOURCE-INSPECTED / INFERRED.
- `` `2026-07-21-rga-job-vs-session-close-uaf-kasan.md` `` — the RGA2 IRQ-thread completion path (`rga_request_release_signal()` dereferencing `job->session`) races `/dev/rga` close freeing the session — KASAN slab-use-after-free on the session object, reproduced on debug build `P70a5-C4ad2` via `rga-session-uaf.sh cross`; a distinct job-vs-session lifetime from the request double-drop (`0052`), fixed by `0057` — MEASURED (KASAN).
- `` `2026-07-21-rga-rewrite-rust-counterfactual.md` `` — should the clean-room RGA rewrite have been written in Rust? No, at this kernel generation: every 2026-07 lifetime-crash finding is in the vendor forward-port track (the rewrite is cited as the reference model, so the greenfield-C ownership design already averted the bug class), and the rewrite's measured center of gravity (201 `iova` / 172 `iommu_` / 138 `dma_fence` / 28 `dma_buf` lines) sits exactly in the subsystems `rust/kernel/` lacks at both the 6.18 and 7.2-rc2 pins — a Rust version would be thousands of lines of hand-rolled unsafe bindings under the lifetime-critical layer, plus DKMS/multi-branch churn costs. Flip trigger recorded: when dma-buf/dma-fence/IOMMU-attach abstractions mainline, revisit a Rust RGA port (rewrite as reference, conformance suite as oracle) and treat a Rust MPP service as the higher-value target — CODE-INSPECTED / INFERRED.
- `` `2026-07-21-mainline-v4l2-vs-vaapi-browser-decode-landscape.md` `` — mainline exposes RK3588 decode via V4L2 stateless (request API), not VA-API: the AV1 IP is mainline since 6.5 and rkvdec2 H.264/H.265 since 7.0 (Collabora), consumed by V4L2-native clients (Chromium's own decoder, GStreamer, ffmpeg request-API hwaccels), while VA-API→V4L2 shims exist but reach neither AV1 nor rkvdec2. Firefox has no native V4L2 decoder, its shipped V4L2 path is stateful M2M (wrong flavor for Rockchip's stateless decoders) with no request-API support planned, and it can't use the ffmpeg rkmpp *wrapper decoders* because its only hardware mechanisms are the VA-API *hwaccel* plus one bespoke v4l2m2m path — so VA-API (the rockchip-vaapi bridge) is Firefox's only Rockchip HW-decode route on either kernel, and a stateless→stateless VA-over-V4L2 AV1 shim would be far easier than the MPP bridge's OBU reconstruction — SOURCE-INSPECTED / INFERRED.
- `` `2026-07-21-vaapi-mpp-bitstream-reconstruction-av1.md` `` — why the VA-API↔MPP bridge finds AV1 far harder than H.264/HEVC/VP9: the bridge reconstructs the bitstream MPP re-parses, and difficulty tracks a reconstruction spectrum — VP9 passes the whole frame through (~0 work), H.264/HEVC regenerate small static parameter sets, but AV1's VA entrypoint hands over only tile data so you must synthesize a large, per-frame, entropy-context-sensitive frame-header OBU every frame (no graceful failure). Not a hardware limit — `av1_rkmpp` decodes AV1 fine because MPP gets real OBUs from the container; the bridge fails only because VA-API already tore the OBUs apart. Rationale for scoping AV1 out of driver v1 — SOURCE-INSPECTED / INFERRED / MEASURED.
- `` `2026-07-21-rockchip-vaapi-driver-review.md` `` — full source review of woodyst/rockchip-vaapi v1.0.11, the first working VA-API-over-MPP driver (LGPL, AI-written, hardware-debugged): strategic architecture matches the ysp bridge sketch and is worth forking, but its two load-bearing shortcuts (per-frame CPU memcpy dodging the DPB/surface-ownership mismatch, usleep-polling sync) must be replaced; effective coverage is Firefox H.264+VP9 (HEVC advertised but headerless → near-certainly non-functional, AV1 honestly disabled, 10-bit exports NV15 as P010); Chromium reach and the four-gate sandbox analysis (path aliasing beats pathname brokers but not Firefox's ioctl-family seccomp filter — `MPP_IOC_CFG_V1` magic `'v'`) are mapped, with a fork-and-renovate ledger of ~4–8 weeks. §9 phase-one (MEASURED): built unmodified against the ysp stack, forked to `yisding/rockchip-vaapi@ysp/cleanup`, cleaned up and packaged; found+fixed three bit-exactness bugs (hardcoded PPS ref counts corrupting multi-ref/B-frame H.264, VP9 backpressure frame drops, VP9 altref PTS mis-routing); H.264 ref×bframes matrix + 4K and VP9 ×10 now bit-exact vs software; unimplemented profiles withdrawn — SOURCE-INSPECTED / MEASURED / INFERRED / UNVERIFIED (browser sandbox internals).
- `` `2026-07-21-ubuntu-rockchip-piggyback-survey.md` `` — Joshua Riek's archived ubuntu-rockchip stack was surveyed source-by-source: its "4K Chromium" is a userspace V4L2-stateful-over-MPP bridge (`libv4l-rkmpp` + patched libv4l2 + udev-created text "device nodes") that is kernel-agnostic and would run on the ysp 6.18 forward-port, but its ~20 Chromium patches target the deleted legacy VDA path and need re-targeting to `V4L2StatefulVideoDecoder`; mpv needs one hwdec-priority patch plus shipped conf defaults, Kodi carries two directly relevant boogie GBM-plane/AFBC patches, VLC and OBS are verified plain rebuilds, gstreamer-rockchip is one repackage away, and the oracular 6.11 branch is a curated pre-6.12 mainline-enablement list with no BSP video at all — SOURCE-INSPECTED / CONFIG-INSPECTED / INFERRED.
- `` `2026-07-21-rga2-dma-api-ownership-and-over-4g-scope.md` `` — implementation plan — now implemented as `0050@473903525009a`/`0051@162edad7bb9c7` — for patches `0050` (map/sync the RGA2 MMU page tables through the DMA API, closing the July 20 ownership finding) and `0051` (map RGA2 job memory against the 32-bit RGA2 device so swiotlb bounces >4G buffers instead of rejecting, with `EOPNOTSUPP` fallback); grounded in the measured 32-bit PTE format, the dormant vendor `use_dma_address` branch, and the upstream/rewrite per-device-mask models — DESIGN / CODE-INSPECTED.
- `` `2026-07-21-rga-ffmpeg-librga-conformance-root-causes.md` `` — the librga smoke's `no core match` is RGA3's 68-pixel minimum intersecting RGA2's below-4G limit on a dma32-heap-less kernel (smoke fixed: CMA fallback + RGA3-minimum sizes; 13 cases green); the AV1 PSNR gap was a missing software reference decoder and now passes bit-exact via dav1d; HEVC Main10→P010 needs AFBC input (linear NV15 strides are not RGA-expressible) and then exposes stock-BSP byte-literal 10-bit stride misprogramming, measured by a direct im2d P010 probe; the legacy `RGA_BLIT` `EFAULT` was a `0045` regression. Kernel fixes `0046`–`0048` (legacy virtual addressing, distinct under-4G `EOPNOTSUPP`, byte-literal 10-bit strides) passed their booted gates on `P63dd-C4ad2` (smoke fully green, ABI/MPP/FFmpeg clean); the P010 gate then exposed the 1 byte/px UV plane-offset defect in `rga_convert_addr()`, fixed by `0049` (booted chroma gate pending) — MEASURED / CODE-INSPECTED.
- `` `2026-07-21-grd-rdp-audio-live-validation-and-codec-control.md` `` — after the completed PipeWire migration and reboot, GRD captured nonzero PCM, sent confirmed `SNDC_WAVE2` packets over `RDPSND`, and the macOS client rendered audible audio; a Windows control rejects playback DVC with the same status and appears to select legacy ADPCM or A-law over SVC rather than AAC, although the exact returned tuple remains unlogged — MEASURED / CODE-INSPECTED / CONFIRMED / INFERRED.
- `` `2026-07-21-rga-forward-port-abi-gaps.md` `` — forward-port patches `0044`/`0045` restore the legacy `RGA2_GET_RESULT` no-op and validate/atomically replace staged RGA task lists, closing both persistent ABI replay gaps plus a repeated-CONFIG allocation leak; booted KASAN debug build `Pb999-C4ad2` passes the full ABI replay (`abi_status=0`) with a clean memory scan — MEASURED / CODE-INSPECTED.
- `` `2026-07-20-rga2-unmapped-page-table-dma-sync.md` `` — direct dma-buf smoke on the KASAN/DMA-debug kernel caught RGA2 passing a `virt_to_phys()` address from ordinary page-table memory to `dma_sync_single_for_device()` without a matching DMA mapping; the page-table allocator/register path needs a retained DMA address and valid device-scoped lifetime — MEASURED / CODE-INSPECTED.
- `` `2026-07-20-rkvenc2-slice-fifo-terminal-drop.md` `` — an aggressive H.264 low-delay split overflows RKVENC2's unchecked 256-entry per-task FIFO, can silently discard the terminal slice, and drives MPP's VEPU580 HAL into an `EIO` retry loop because it ignores the poll error and tests an uninitialized `slice_last`; safe conformance defaults pass, but both kernel and MPP need hardening — MEASURED / CODE-INSPECTED.
- `` `2026-07-20-armbian-non-radxa-radxa-uboot-audit.md` `` ([evidence](evidence/2026-07-20-armbian-non-radxa-radxa-uboot-audit/README.md)) — all 203 currently linked non-Radxa images whose resolved config uses `radxa/u-boot` were scoped against issue #8227 without full downloads: 17 have a zero-byte FIT DTB (Banana Pi M5 Pro 1, IMB3588 4, MekoTronics R58 4x4 4, NanoPC T6 LTS 4, Orange Pi 5 Plus 4), 182 are clean, and four R58HD aliases are dead HTTP 404 links — MEASURED / CONFIG-INSPECTED / SOURCE-INSPECTED.
- `` `2026-07-20-armbian-radxa-image-fit-audit.md` `` ([evidence](evidence/2026-07-20-armbian-radxa-image-fit-audit/README.md)) — all 323 images linked from Armbian's 38 Radxa board pages were scoped against issue #8227 without full downloads: 21 have a zero-byte FIT DTB (CM5 IO 4, E24C 4, ROCK 2A 4, ROCK 5B 9), 207 streamed artifacts are clean, and 95 mainline or other-family images are not applicable — MEASURED / CONFIG-INSPECTED / SOURCE-INSPECTED.
- `` `2026-07-20-grd-rdp-audio-split-stack.md` `` — GRD fell back to static `RDPSND` and negotiated PCM, but applications played through standalone PulseAudio while GRD saw no native PipeWire audio sink; the product stack must install `pipewire-audio`, and post-migration RDP playback remains the gate — MEASURED / CODE-INSPECTED / CONFIG-INSPECTED / CONFIRMED.
- `` `2026-07-20-grd-rdpgfx-focus-resume-ack-wedge.md` `` — switching away from macOS Windows App caused the client to suspend RDPGFX acknowledgements; on return GRD reconstructed a 908-frame decoded deficit as unacknowledged history, left the renderer at zero frame slots, and never escaped although input/transport and hardware remained healthy. Cleaned patch `0018@34145d9` preserves normal resume throttling but clears only stalled reconstructed history after two seconds without `totalFramesDecoded` progress and forces a full refresh; its functionally equivalent installed `exp6` predecessor fired once in the live focus workload and restored hardware submissions from 7,758 to continued progress — MEASURED / CORE-INSPECTED / SOURCE-INSPECTED / RUNTIME-RECOVERY-VALIDATED.
- `` `2026-07-20-grd-focus-return-false-pipeline-starvation.md` `` — a GDB trigger proved the first newly outstanding view after a 42-second macOS focus-away interval was charged against the last pre-idle hardware submission, immediately tripping patch `0016`'s software cooldown even though the fresh work was about 1 ms old. Patch `0019@3e4480e` timestamps the drained-to-outstanding transition so idle time cannot masquerade as a stall; source and arm64 package builds pass and live validation remains — GDB-INSPECTED / LOG-CORRELATED / SOURCE-FIXED.
- `` `2026-07-19-grd-rkmpp-encoder-wedge-mpp-input-backpressure.md` `` — sustained 60 fps can momentarily exhaust MPP's input task pool; the deployed FFmpeg wrapper waited for output from the refused, never-submitted frame and surfaced a timeout as hardware failure. The fix belongs in the wrapper, not GRD: `da5befc806` retries the refused synchronous put within one 500 ms deadline and maps an elapsed packet wait to `EAGAIN`. It compiles and is Published in the normal PPA; the live RDP stress re-test remains pending — MEASURED / SOURCE-INSPECTED / COMPILE-VERIFIED.
- `` `2026-07-19-grd-rkmpp-encoder-wedge-userspace-not-driver.md` `` — driver task tracing at the exact failure second and a concurrent `mpi_enc_test` show GRD's frames completing normally in rkvenc2 while FFmpeg reports `AVERROR_EXTERNAL`, exonerating the kernel/hardware and narrowing the wedge to MPP/FFmpeg packet delivery; the exact mechanism was subsequently pinned as MPP input backpressure in the newer finding above — MEASURED / CONFIRMED / INFERRED.

- `` `2026-07-18-grd-starvation-detector-diagnostic-only-no-recovery.md` `` — the gnome-remote-desktop RDP session wedges under a busy desktop (e.g. YouTube); traced live with gdb + matched symbols to the "GRD EGL thread" spinning at 99.9% CPU in Mesa `convert_ubyte`, i.e. `download_in_impl`'s `glReadPixels(GL_BGRA)` reading the imported linear dma-buf that panthor maps **uncached**, so Mesa's per-pixel format-convert fallback crawls (seconds-minutes/frame) and the view-creation thread blocks on it. Fixed consumer-side by patch `0017` (GPU copy into a cached scratch texture, then read that back); the earlier starvation-recovery patch `0016` was based on a wrong premise and does not fix it. The two-gate deadlock and VEPU-starvation framings were symptoms — MEASURED / SOURCE-INSPECTED / CONFIRMED.
- `` `2026-07-18-mesa-panfrost-uncached-readpixels-convert-cliff.md` `` — two upstream Mesa bugs exposed by the above: (A) the core `glReadPixels` convert fallback (`_mesa_format_convert`/`convert_ubyte`) reads the source map **per-pixel** with no `streaming-load-memcpy` bounce, catastrophic when the source is WC/uncached; (B) panfrost maps **imported linear** buffers directly+uncached for CPU readback instead of the cached staging blit it already uses for AFBC/AFRC — panthor treats imports as uncached (`panthor_kmod.c:471`). Either fix removes the cliff for all GL clients; suitable for filing upstream — MEASURED / SOURCE-INSPECTED.
- `` `2026-07-18-rkvenc2-wait-result-task-uaf-kasan.md` `` — KASAN showed the forward-port's `rkvenc2_wait_result()` freeing the encoder task through `rkvenc2_task_default_process()` and then reading `task->state`; patch `0043` samples the abort flag before the final reference drop. It is exported and KASAN-clean, while corrected isolated and full official-MPP functional runs now pass; the independently exposed slice-FIFO overflow is tracked above — MEASURED / CODE-INSPECTED / CONFIRMED.
- `` `2026-07-21-rga-request-completion-vs-session-close-uaf-kasan.md` `` — **distribution blocker.** On the current tip (`162edad7bb9c7`, `0044`–`0051`), the `cross` session-close reproducer under KASAN tripped a *new* slab-use-after-free distinct from the buffer force-free `0040` fixed: the RGA2 IRQ completion thread reads `&request->finished_wq.lock` in `wake_up()` (`rga_request_release_signal`) after `/dev/rga` close freed the `rga_request` via `rga_request_session_destroy_abort` → `rga_request_kref_release` → `kfree`, with a concurrent `refcount_t: underflow` in the same function — root-caused (full reference-model trace) to a double-drop of the request's single initial `rga_request_alloc()` reference by four unserialised retire paths. Fixed by `0052@c46bfd6622ba6` (idempotent `rga_request_release_ref()` under the manager lock); booted gate (a quiet `cross` run with `async_submits > 0`) pending the P9c12 build — MEASURED (UAF + underflow, full stacks) / CODE-INSPECTED / INFERRED (double-put root cause).
- `` `2026-07-21-mpp-collect-msgs-clientless-session-null-deref-crash.md` `` — **second distribution blocker, fatal (hard lockup), now with a repeatable trigger.** The `vp90-2-10-show-existing-frame2.webm` VP9 vector corrupts MPP/`rk_vcodec` buffer state through two independent userspace paths (the `rockchip-vaapi` VA-API driver **segfaults**; a direct RKMPP decode throws **invalid ref-count / buffer-slot assertions + leaked buffers**), and ~47 s later — a *deferred*, non-synchronous fault — the kernel oopsed: `Unable to handle kernel paging request at dfff800000000363` (KASAN-shadow fault, near-NULL deref). Root-cause candidate: the async worker `mpp_task_worker_default()` (`mpp_common.c:1003`) derefs `task->session->mpp` **unguarded**, unlike the ioctl path at `:615` which NULL-checks it — so a pending task whose session's client was torn down (`not find client 0`) NULL-faults in the worker. The vector produces **three distinct bugs**: leg 1 = the `rockchip-vaapi` driver under-allocating vs MPP's 768-byte stride (the `show_existing_frame` reference is larger than the nominal 352×288) → CPU-copy overrun → **segfault**, now **fixed userspace-side** (track 14, `frame_layout.c`); leg 2 = MPP-core (`librockchip_mpp`) VP9 buffer-slot/refcount mismanagement → the RKMPP assertions/leaks, **still open** (the decode-side root cause); leg 3 = the kernel worker `mpp_task_worker_default()` (`mpp_common.c:1003`) NULL-derefing `task->session->mpp` for a device-less session → the hard lockup, **fixed** by `0053@98232d5c06fab` + `0054@e4c9b62669526` (worker + orphan pop/free/running + the wait-result sibling; `pop_pending` no longer refuses NULL-device tasks, which had spun+leaked the abort path). Legs distinct in mechanism/memory-domain (leg 1 MMU-caught userspace segfault vs leg 3 kernel NULL-deref, ~47 s deferred) — high confidence, not certain (no kernel trace: ramoops doesn't persist here). Gate needs serial/netconsole + the KASAN reproducer — MEASURED (oops + repeatable trigger + timing + userspace asserts) / STRONGLY INFERRED (leg-3 crash site) / UNPROVEN at PC.
- `` `2026-07-21-ramoops-not-preserved-across-warm-reset-rk3588.md` `` — **why we keep getting no crash trace.** ramoops/pstore is configured correctly on the debug kernels but never captures anything: a deliberate `echo c` that *self-rebooted* (clean warm `panic=10` reset, no power-cycle) still left `/sys/fs/pstore` empty — including the continuously-written console zone — so the reserved DRAM region is not surviving the reset. RK3588 re-initializes/re-trains DRAM on every `SYSTEM_RESET`, and this Armbian firmware stack (`ddr-v1.20 / bl31-v1.48 / uboot-rmbian`) does not preserve the `0x118000` BSP window the way Rockchip BSP firmware does (the patch inherited the address but not the firmware guard). Unresolved: full-DRAM-loss vs targeted low-window clobber. Fix: capture off-board (serial `ttyS2` @1500000, or netconsole) — MEASURED (self-reboot → empty pstore incl. console zone) / INFERRED (DDR-reinit mechanism) / UNVERIFIED (whether any address survives).

- `` `2026-07-18-mpp-reset-session-dma-double-free-kasan.md` `` — the KASAN+ramoops rebuild (`P712f-C40aa`) caught the preflight Oops on the first narrowed pass and proved it is neither a procfs race nor an incomplete `0041`: `MPP_CMD_RESET_SESSION` destroyed `session->dma` without clearing the pointer, so later async teardown freed it again. Patch `0042` adds the missing NULL, and rebuilt KASAN run `20260718-093751-kasan-narrowed` exercised RESET_SESSION with zero flagged kernel lines; the separate `0043` encoder UAF is also resolved, while production packaging/full conformance remain open — MEASURED / CODE-INSPECTED / CONFIRMED.
- `` `2026-07-17-forward-port-conformance-preflight-oops.md` `` — the newly booted 6.18.38 PPA forward-port kernel (including session fixes `0040`/`0041`) Oopsed and auto-rebooted during the MPP suite's initial recursive `/proc/mpp_service` snapshot, immediately after ABI replay and before any media case began; the journal preserved a random-address level-0 translation fault but no PC/LR/trace, while pstore was empty because ramoops was not configured, so another MPP procfs lifetime race is the leading but unproven attribution and a KASAN+ramoops narrowed reproduction is now the gate. **Superseded 2026-07-18** by `2026-07-18-mpp-reset-session-dma-double-free-kasan.md`, which reproduced under KASAN and showed the fault is a `RESET_SESSION` `session->dma` double-free during ABI replay, not a procfs snapshot race — MEASURED / INFERRED (attribution overturned).
- `` `2026-07-17-rk3588-maxline-implementation-and-build-record.md` `` — promoted → [`../packaging/ppa/kernel-maxline/verification.md`](../packaging/ppa/kernel-maxline/verification.md) (2026-07-19).
- `` `2026-07-17-rk3588-maximum-mainline-kernel-plan.md` `` — promoted → [`../packaging/ppa/kernel-maxline/integration-design-record.md`](../packaging/ppa/kernel-maxline/integration-design-record.md) (2026-07-19).
- `` `2026-07-17-mpp-procfs-session-teardown-oops.md` `` — high-frequency `/proc/mpp_service/sessions-summary` sampling raced RKVENC teardown and produced a captured NULL dereference in `rkvenc_dump_session()` → `rwsem_read_trylock`: procfs held `session_lock`, but teardown freed `session->priv` and `session->dma` before taking that lock and unlinking the session; forward-port fix `@df0d7037213c` unlinks at common deinit entry before private teardown, removes redundant late unlinks, and defensively guards the encoder dump; three affected arm64 objects compile, but boot/hardware validation remains pending — MEASURED / CODE-INSPECTED / COMPILE-VERIFIED.
- `` `2026-07-17-rga-session-close-uaf.md` `` — an ABI-probe test leak chained into a kernel Oops: `RGA_IOC_IMPORT_BUFFER` returns the positive handle as its retval, so the dma-buf probe's `if (!ret)` never released it (fixed to `if (ret >= 0)`), and on `/dev/rga` close `rga_mm_session_release_buffer()` force-frees session buffers ignoring their kref, so a buffer still referenced by another session (imports dedup globally) or an in-flight job is freed underneath a live reference; driver fix `linux-6.18-rkvenc-av1-fwport@bc086cbe03d7` drops the reference through the kref path and detaches `->session` when a reference survives — the exact faulting function is unproven (journal truncated, pstore empty) — MEASURED (the Oops) / CODE-INSPECTED / INFERRED (attribution).
- `` `2026-07-16-rockchip-bsp-driver-quality.md` `` — Rockchip's BSP is feature-strong and functionally effective on fixed hardware, but its private MPP/RGA/RKNPU stacks remain below mature mainline drivers in hostile-input validation, resource lifetimes, client isolation, framework integration, review traceability, and branch maintenance; the RKNPU deep dive finds a capable fixed vendor stack whose render-node ABI exposes/trusts kernel object pointers while the matching compiler/runtime are proprietary prebuilts, whereas upstream-derived areas such as DRM display are materially healthier — CODE-INSPECTED / SOURCE-INSPECTED / MEASURED / INFERRED for comparative ratings.
- `` `2026-07-13-rock5b-u-boot-fit-dtb-race.md` `` — controlled delay proves the missing FIT prerequisite; current Armbian Jammy and Noble images use the same Make 4.3 but coreutils 8.32 uses direct read/write while 9.4 first tries FICLONE and copy_file_range; KSpace exposed 64 Docker CPUs, 24 co-located runners, a host-bind-mounted U-Boot worktree, and an inferred `-j96`, while its backing filesystem remains unknown; includes the [source/trace appendix](evidence/2026-07-13-u-boot-fit-dtb-race/coreutils-copy-path-comparison.md), Mac container precedents, and a ready one-line Armbian test patch — MEASURED / SOURCE-INSPECTED / CONFIG-INSPECTED / INFERRED.
- `` `2026-07-11-rock5b-u-boot-four-way-comparison.md` `` — promoted → [`../boot-firmware/docs/version-comparison.md`](../boot-firmware/docs/version-comparison.md) (2026-07-11).
- `` `2026-07-11-kodi-ffmpeg-rockchip-hwaccel.md` `` — stock Kodi 22 can auto-select the fork's RKMPP decoders through DRM PRIME without a Kodi patch; the PPA MPP runtime fixes missing parser registration, three FFmpeg packaging failures were fixed, and the AV1 MP4/MKV extradata flag fix is built/published but still needs an RK3588 playback re-test — MEASURED, except for the pending hardware re-test.
- `` `2026-07-09-ffmpeg-baseline-rk2-local-build-validation.md` `` — locally regenerated the `ffmpeg 7:8.1.2-1+rk2` source package from the byte-identical 8.1.2 orig tarball plus `packaging/ppa/ffmpeg-baseline/debian`, extracted that `.dsc`, and completed an arm64 binary build on the ROCK 5B; `fate-source` passed, `fate-filter-frei0r-filter` and `fate-filter-frei0r-filter-unaligned` both ran without the prior `Could not find module 'distort0r'` failure, 32 binary artifacts were produced, and the built standard binary exposes the expected RKMPP encoders/decoders; caveat: source-package accurate but not a clean Launchpad `sbuild` replica because `frei0r-plugins` was provided via extracted package + `FREI0R_PATH`, and local builds under `downloads/` need `GIT_CEILING_DIRECTORIES` to keep upstream `fate-source` from seeing the enclosing repo — MEASURED.
- `` `2026-07-09-rock5b-armbian-sd-boot-investigation.md` `` — consolidated ROCK 5B Armbian SD boot investigation: original 26.2.1 raw SD path stops after BL31 with `ddr-v1.18` and no HDMI, SD image/rootfs/`/boot` verify cleanly, 26.5.1 vendor kernel transplant does not fix the intact raw-loader case, zeroing SD sectors 64..32767 makes the same SD rootfs boot through known-good SPI, 26.5.1 vendor raw U-Boot initializes HDMI but still does not complete boot, and the next discriminator is writing the known-good-family 26.5.1 `current` raw artifacts to SD — MEASURED / SOURCE-INSPECTED / HYPOTHESIS for exact raw-loader preemption mechanism.
- `` `2026-07-08-armbian-26.2.1-bl31-handoff-hang.md` `` — Armbian 26.2.1 Minimal Debian 13 / 6.1.115 on ROCK 5B uses Armbian build commit `5abb97453`, Radxa U-Boot `6c807...`, RK3588 DDR blob `v1.18`, and a vendor U-Boot config with `CONFIG_DISABLE_CONSOLE=y`, so the UART stop after BL31 handoff does not by itself prove BL33 crashed; the card write and `/boot` payload verify cleanly, a 26.5.1 vendor kernel/initrd transplant still failed, disabling `/boot/boot.scr` still hung instead of falling through to NVMe, but backing up and zeroing the SD raw-loader gap at sectors 64..32767 made the SD boot successfully via the known-good SPI path; Radxa source diff `6c807...`→`39cd...` does not touch ROCK 5B files, while Armbian `1bac6d97` switches RK3588 DDR blob v1.18→v1.20 — MEASURED / SOURCE-INSPECTED / HYPOTHESIS for exact raw-loader preemption mechanism.
- `` `2026-07-08-blit-precision-nir-migration.md` `` — proposed NIR design for rebuilding TXF/TEX coordinates from exact fragment position and flat affine coefficients; its IR analysis remains valid, but its causal premise and claim to be the proper general fix are **corrected by the 2026-07-22 hardware-erratum finding**: zero-valued depth bias repairs both raw varying and ordinary TEX without coordinate reconstruction. The design is therefore an avoidance strategy, not the established root-cause workaround — CODE-INSPECTED (diff) / DESIGN (proposed avoidance; causal attribution superseded).
- `` `2026-07-08-arm-mali-blob-interp-drift-bit-identical-to-mesa.md` `` — promoted → [`video-libraries/mesa/docs/arm-mali-blob-stack.md`](../video-libraries/mesa/docs/arm-mali-blob-stack.md) "Runtime Results" (2026-07-08). ARM blob drift is bit-identical to Mesa → the drift is hardware.
- `` `2026-07-08-armbian-builder-setup.md` `` — the YSP ROCK 5B Armbian builder is the Noble 24.04 aarch64 VMware VM (5 vCPU / 7.7 GiB / root LV grown 48→97 GB), a supported *native* `armbian/build` host (arm64→arm64, `gcc 13.3.0`, no QEMU; `compile.sh` self-relaunches under sudo, no passwordless sudo); for `rock-5b` (family `rockchip-rk3588`) the branch map is legacy 5.10 / vendor 6.1 BSP (`rk-6.1-rkr5.1`→6.1.115) / **current = 6.18 mainline (default, the forward-port target)** / edge 7.1 / bleedingedge 7.2, and Ubuntu 26.04 = `resolute` (`supported`); an unpatched `kernel` build pulls prebuilt `.deb`s from `ghcr.io/armbian/os/…` in 0:19 so only `ARTIFACT_IGNORE_CACHE=yes` or a userpatch hash-change forces local compilation, and 8 GB RAM clears Armbian's BTF gate by only ~19–45 MiB so BTF-link survival is unproven — MEASURED / CONFIG-INSPECTED / BTF-risk HYPOTHESIS.
- `` `2026-07-08-arm-mali-blob-gbm-setversion-kernel-oops.md` `` — promoted → [`video-libraries/mesa/docs/arm-mali-blob-stack.md`](../video-libraries/mesa/docs/arm-mali-blob-stack.md) "Runtime Results" (2026-07-08). GBM path NULL-derefs `drm_setversion` and wedges DRM; use the X11-client reproducer.
- `` `2026-07-07-arm-mali-blob-reproducer-readiness.md` `` — promoted → [`video-libraries/mesa/docs/arm-mali-blob-stack.md`](../video-libraries/mesa/docs/arm-mali-blob-stack.md) "Runtime Results" (2026-07-08). GLES stack wired, link `-lmali`, Vulkan unavailable from the g6p0 repos.
- `` `2026-07-07-rock5b-spi-sd-boot-chain.md` `` — ROCK 5B SPI contains a valid Armbian/Radxa U-Boot image matching the packaged `rkspi_loader.img`; the Radxa Bookworm SD card has bootable p2/p3, p3 contains standard extlinux with matching root UUID and a Rock 5B DTB, so SPI U-Boot should be able to attempt the SD kernel, but the later Armbian SD experiment showed the "SPI bypasses raw SD loader" assumption is not reliable: zeroing the SD raw-loader gap can be necessary to force the known-good SPI path — MEASURED / INFERRED where noted.
- `` `2026-07-06-rga3-dmabuf-scatter-bsp-contract.md` `` — promoted → [`kernel-drivers/rga/userptr-iommu.md`](../kernel-drivers/rga/userptr-iommu.md) §5 (2026-07-08). dma-buf single-span DESIGN-CONSTRAINT vs BSP.
- `` `2026-07-06-ffmpeg-rockchip81-package-validation.md` `` — promoted → [`video-libraries/ffmpeg/docs/rockchip81-package-validation.md`](../video-libraries/ffmpeg/docs/rockchip81-package-validation.md) (2026-07-08). Package build + on-board smoke passes and the four failures (incl. the incomplete-`/usr`-MPP decode root cause).
- `` `2026-07-05-rkvenc-rcb-sram.md` `` — promoted → [`kernel-drivers/mpp/docs/rcb-sram.md`](../kernel-drivers/mpp/docs/rcb-sram.md) (2026-07-08). RKVENC RCB is ABI-plumbed but not SRAM-backed in DT; keep encoder RCB alloc best-effort, don't borrow decoder SRAM.
- `` `2026-07-05-rga3-userptr-iommu-runtime-smoke.md` `` — promoted → [`kernel-drivers/rga/userptr-iommu.md`](../kernel-drivers/rga/userptr-iommu.md) §6 + Status (2026-07-08). Six 18:28 smoke runs pass; fallback attribution still indirect.
- `` `2026-07-05-rga3-userptr-iommu-design.md` `` — promoted → [`kernel-drivers/rga/userptr-iommu.md`](../kernel-drivers/rga/userptr-iommu.md) §4 (2026-07-08). Driver-owned contiguous-IOVA fallback design (guard band, `iommu_map_sg`, 32-bit/granule guards).
- `` `2026-07-05-rga3-scattered-iova-mechanism.md` `` — promoted → [`kernel-drivers/rga/userptr-iommu.md`](../kernel-drivers/rga/userptr-iommu.md) §3 (2026-07-08). Descending-IOVA fingerprint (`contiguous=0`), per-segment-not-coalesced; bounce trigger UNRESOLVED.
- `` `2026-07-05-rga3-memory-import-contract.md` `` — promoted → [`kernel-drivers/rga/userptr-iommu.md`](../kernel-drivers/rga/userptr-iommu.md) §2 (2026-07-08). Import-contract ABI (fd/userptr/phys/mmap); superseded-for-implementation by §4.
- `` `2026-07-04-librga-consumer-survey.md` `` — promoted → [`kernel-drivers/rga/userspace-consumers.md`](../kernel-drivers/rga/userspace-consumers.md) (2026-07-08). Public consumer survey (RKNN-dominated required profile; RGA2-Pro/FBC tail modes rejected `-EOPNOTSUPP`).
- `` `2026-07-04-rga3-im2d-error-irq.md` `` — promoted → [`kernel-drivers/rga/userptr-iommu.md`](../kernel-drivers/rga/userptr-iommu.md) §1 (2026-07-08). `INTR[0x2]` MMU-IRQ root cause + the three BSP-derived fixes (`13afe70c8271`/`6b9dba7abcd0`/`590c9ef297ce`).
