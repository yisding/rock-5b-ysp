commit 56e403ede0812f31741b173c89aa8cf93f7f2a6c
Author: rkvenc forward-port <forward-port@local>
Date:   Mon Jun 29 02:14:51 2026 -0700

    WIP: BSP audit cleanup edits (machine-generated, compile-tested)
diff --git a/drivers/video/rockchip/mpp/mpp_rkvdec2_link.c b/drivers/video/rockchip/mpp/mpp_rkvdec2_link.c
index f52df09f0547..759f19de83b5 100644
--- a/drivers/video/rockchip/mpp/mpp_rkvdec2_link.c
+++ b/drivers/video/rockchip/mpp/mpp_rkvdec2_link.c
@@ -427,7 +427,7 @@ static void rkvdec_link_reg_dump(const char *func, struct rkvdec_link_dev *dev)
 	mpp_err("reg 4 %08x - decoded num\n", dev->decoded_status);
 	mpp_err("reg 5 %08x - total num\n", dev->total);
 	mpp_err("reg 6 %08x - link mode en\n", dev->enabled);
-	mpp_err("reg 6 %08x - next ltb addr\n", dev->iova_next);
+	mpp_err("reg 7 %08x - next ltb addr\n", dev->iova_next);
 }
 
 static void rkvdec_link_counter(const char *func, struct rkvdec_link_dev *dev)
@@ -849,8 +849,10 @@ int rkvdec2_link_init(struct platform_device *pdev, struct rkvdec2_dev *dec)
 	/* alloc table pointer array */
 	table = devm_kmalloc_array(mpp->dev, mpp->task_capacity,
 				   sizeof(*table), GFP_KERNEL | __GFP_ZERO);
-	if (!table)
-		return -ENOMEM;
+	if (!table) {
+		ret = -ENOMEM;
+		goto done;
+	}
 
 	/* init table array */
 	link_dec->table_array = table;
@@ -883,6 +885,7 @@ int rkvdec2_link_init(struct platform_device *pdev, struct rkvdec2_dev *dec)
 done:
 	if (ret) {
 		if (link_dec) {
+			rkvdec2_link_remove(mpp, link_dec);
 			if (link_dec->reg_base) {
 				devm_iounmap(dev, link_dec->reg_base);
 				link_dec->reg_base = NULL;
@@ -1532,8 +1535,12 @@ int rkvdec2_attach_ccu(struct device *dev, struct rkvdec2_dev *dec)
 	mpp_debug_enter();
 
 	np = of_parse_phandle(dev->of_node, "rockchip,ccu", 0);
-	if (!np || !of_device_is_available(np))
+	if (!np)
 		return -ENODEV;
+	if (!of_device_is_available(np)) {
+		of_node_put(np);
+		return -ENODEV;
+	}
 
 	pdev = of_find_device_by_node(np);
 	of_node_put(np);
@@ -1554,8 +1561,10 @@ int rkvdec2_attach_ccu(struct device *dev, struct rkvdec2_dev *dec)
 	}
 
 	ret = of_property_read_u32(dev->of_node, "rockchip,core-mask", &dec->core_mask);
-	if (ret)
+	if (ret) {
+		put_device(&pdev->dev);
 		return ret;
+	}
 	dev_info(dev, "core_mask=%08x\n", dec->core_mask);
 
 	/* if not the main-core, then attach the main core domain to current */
@@ -1586,7 +1595,14 @@ int rkvdec2_attach_ccu(struct device *dev, struct rkvdec2_dev *dec)
 	dec->ccu = ccu;
 
 	dev_info(dev, "attach ccu as core %d\n", dec->mpp.core_id);
-	mpp_debug_enter();
+	mpp_debug_leave();
+
+	/*
+	 * Drop the of_find_device_by_node() lookup reference; the ccu device
+	 * stays alive via its own driver binding (dec->ccu is its drvdata).
+	 * Mirrors the rkvenc_attach_ccu() success path.
+	 */
+	put_device(&pdev->dev);
 
 	return 0;
 }
@@ -2403,7 +2419,7 @@ static int rkvdec2_hard_ccu_reset(struct mpp_taskqueue *queue, struct rkvdec2_cc
 
 	/* reset and active core */
 	for (i = 0; i < queue->core_count; i++) {
-		u32 val = 0;
+		u32 val;
 		struct mpp_dev *mpp = queue->cores[i];
 		struct rkvdec2_dev *dec = to_rkvdec2_dev(mpp);
 
@@ -2417,17 +2433,11 @@ static int rkvdec2_hard_ccu_reset(struct mpp_taskqueue *queue, struct rkvdec2_cc
 
 		{
 			/* soft reset */
-			u32 val;
-
 			mpp_write(mpp, RKVDEC_REG_IMPORTANT_BASE, RKVDEC_SOFTREST_EN);
 			udelay(5);
 			val = mpp_read(mpp, RKVDEC_REG_INT_EN);
 			if (!(val & RKVDEC_SOFT_RESET_READY))
 				mpp_err("soft reset fail, int %08x\n", val);
-
-			// /* cru reset */
-			// dev_info(mpp->dev, "cru reset\n");
-			// rkvdec2_reset(mpp);
 		}
 #if IS_ENABLED(CONFIG_ROCKCHIP_SIP)
 		rockchip_dmcfreq_lock();
@@ -2584,7 +2594,7 @@ static int rkvdec2_hard_ccu_enqueue(struct rkvdec2_ccu *ccu,
 			struct rkvdec2_dev *dec = to_rkvdec2_dev(core);
 			struct rkvdec_link_dev *link_dec = dec->link_dec;
 
-			if (mpp->disable)
+			if (core->disable)
 				continue;
 			work_mode |= dec->core_mask;
 			rkvdec2_ccu_link_fix_rcb_regs(dec);
