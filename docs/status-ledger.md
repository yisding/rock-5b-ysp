# Status ledger

This is the audit companion to [`../status.md`](../status.md). Keep
`status.md` as the status dashboard and watchlist; use this file for the
cross-project note behind each track — what the current state rests on, and
which boundary is still open.

Each row states **current** state only. The superseded chain — every earlier
explanation, incident, and retracted premise — belongs to the dated findings
linked in the last column, which own the experiment and its trust tags. Do not
re-narrate that history here; a row that grows into a chronology has stopped
being an audit companion. Canonical mechanism still belongs in the owning
project docs.

| # | Track | Longer dated note | Last verified | Canonical detail |
|---|-------|-------------------|---------------|------------------|
| 1 | Kernel forward-port | Public `rk3588-video-6.18` and the exported series are aligned at `5b87d46eefdcb` / `0001`–`0087`. Signed source `6.18.41+rk3588av1fwport20260802-0ubuntu1~rk1` passed patch-only staging, signature and extracted-source provenance/config checks; source `18654047`, binaries `247715541`–`247715543`, and arm64 build `33461848` are Published/successful. The `0001`–`0080` predecessor is Published as `18652965` with build `33460058` successful. None of the seven newest commits has booted hardware evidence, so installation, boot identity, conformance, and rollback are all open for this package. Two resolved premises still shape the series and must not be re-derived: the GRD first-frame scatterlist oops was **not** driver corruption but `CONFIG_DMABUF_DEBUG`'s `mangle_sg_table()`, a 100% upstream defect reported since 2022 and now disabled in the tracked config; and the `…20260725` orig was provenance-contaminated by rewrite-tree files, which is why the exporter now excludes `drivers/video/rockchip/*-rewrite/` and the source was re-cut. | 2026-08-02 | [`packaging/ppa/kernel-forward-port/README.md`](../packaging/ppa/kernel-forward-port/README.md), [`kernel-drivers/docs/forward-port-status.md`](../kernel-drivers/docs/forward-port-status.md), [dma-buf root cause](../findings/2026-07-28-dmabuf-debug-mangle-sg-table-is-the-sg-writer.md), [upstream provenance](../findings/2026-07-28-dmabuf-debug-upstream-provenance-and-fix-options.md), [orig contamination](../findings/2026-07-29-production-6-18-40-orig-is-rewrite-composite-snapshot.md), [ISR fault-handler panic](../findings/2026-07-29-mpp-isr-fault-handler-clear-sleeps-panics-idle-task.md), [6.18.40 KASAN validation](../findings/2026-07-25-forward-port-6-18-40-kasan-full-validation.md) |
| 2 | BSP-audit fixes | The audit's 16 HIGH reviewer rows collapse to 13 distinct bugs: two were already absent from the later forward-port and the remaining 11 were semantically ported as `0058`–`0068`. Booted KASAN/lockdep package `Pabd5-C4ad2` passed the broad codec/ABI suites and the destructive foreign-fd, physical-import, reset-session, clientless-release, and cross-session paths; later `0071`-tip root gates and the Published `…20260723~rk1` production kernel's full conformance/root run were also green. That evidence validates the integrated tail, **not** every HIGH fix in isolation: acquire-fence cleanup, queued-job shutdown, missing-plane rejection, and partial-handle unwind still need targeted hostile-path gates. The historical 65-patch MEDIUM/LOW cleanup series remains unshippable — its strengthened form diverges from the verified draft and patch `0024` lacks a forward declaration. | 2026-07-24 | [port record](../findings/2026-07-22-bsp-high-current-tip-port.md), [production run](../findings/2026-07-24-production-ppa-kernel-full-conformance-run.md), [`kernel-drivers/docs/bsp-audit.md`](../kernel-drivers/docs/bsp-audit.md), [`cleanup-split`](../kernel-drivers/patches/cleanup-split/README.md) |
| 3 | DKMS channel | The stock-kernel DKMS package compiles and links on the documented 6.18 target. The DT overlay is `dtc`-validated but not boot-validated, and the channel is mutually exclusive with the combined built-in kernel. | 2026-07-01 | [`packaging/dkms/README.md`](../packaging/dkms/README.md) |
| 4 | Clean-room rewrite drivers | The 2026-08-02 adversarial review found fifteen implementation defects and repaired all fifteen, spanning DMA ownership and double-copyback, a partially created mapping torn down after its power domain suspended, request-ID reuse redirecting cleanup, driver-owned RCB IOVAs failing their own provenance check, a powered-off HARD-CCU link register read, teardown after a reset that never proved DMA stopped, poll-batch restart and multi-poll overwrite, and a missing `CONFIG_SYNC_FILE` select. Porting that work to mainline exposed a sixteenth defect — an IOMMU-IRQ-mask recovery fallback unreachable by construction — whose removal (`501a2b47f3503`) is a no-op on 6.18 because the helper is BSP-only. Tips are 6.18 `501a2b47f3503` and mainline `694aac9b7c0ff`, byte-identical again, with all four object builds passing across 6.18 and `v7.2-rc5`. Evidence lags the source in both directions: the cleanest booted result `#29` (`g8042f13c5459`) posted exact 89/89 MPP plus 150/150 RGA KTAP with live lockdep and every expected core registered, but **predates** the review fixes; KASAN package `#30` (`g2f05724a2003`) carries them, is built and installed at `/boot`, has **never booted**, and itself predates `501a2b47f3503` — so a successor package is required. Qualification needs the exact ordered case manifest plus matching source/config/package identity. No AV1 hardware run, librga suite, or SWIOTLB/recovery fault injection exists on the corrected source, and VCD completion still lacks an independent architectural AFBC DMA-retirement proof. | 2026-08-03 | [adversarial review](../kernel-drivers/docs/rewrite-driver-adversarial-review-2026-08-02.md), [unreachable IRQ mask](../findings/2026-08-03-rewrite-rga-unreachable-iommu-irq-mask.md), [review round 3](../findings/2026-08-01-rewrite-driver-review-round-3.md), [AV1/VSI lifecycle](../findings/2026-07-30-rewrite-av1-vsi-fault-afbc-lifecycle-races.md), [`kernel-drivers/docs/rewrite-kunit.md`](../kernel-drivers/docs/rewrite-kunit.md), [`kernel-drivers/tests/rewrite-conformance.md`](../kernel-drivers/tests/rewrite-conformance.md), [retrospective](../findings/2026-08-01-rewrite-driver-retrospective.md) |
| 5 | ffmpeg tree | Canonical `main`, `ffmpeg-80`, and `ffmpeg-81` tips retain source/FATE-only proof; broad package/encode/RGA smoke remains historical `75638e7f0b17`. The normal-system package branch advances to `fix/rkmpp-output-timeout@c9428bedaa`, which fixes the asynchronous `MppFrame` reset/close double release: nonblocking MPP owns a submitted frame until packet return or teardown, while the old close path destroyed MPP and then deinitialized the same stored pointer. Affected-object and `fate-source` builds, source construction/checksum verification, and focused RK3588 gates pass — 10/10 immediate close and 10/10 flush/reuse with no refcount, frame-pool, or kernel-fault line. Current `nyanmisaka/ffmpeg-rockchip@388741a3544b` shares the defect; mainline FFmpeg `a441a2eb3839` uses a distinct blocking implementation and does not. Published/installed packages remain at predecessor `33a651a55b`; candidate installation, the real GRD fallback/recreation gate, canonical-tip hardware, and AV1 MP4/MKV validation are open. | 2026-07-30 | [lifetime fix](../findings/2026-07-30-ffmpeg-rkmpp-async-frame-lifetime-fix.md), [`video-libraries/ffmpeg/README.md`](../video-libraries/ffmpeg/README.md), [input backpressure](../findings/2026-07-19-grd-rkmpp-encoder-wedge-mpp-input-backpressure.md), [AV1 follow-up](../findings/2026-07-11-kodi-ffmpeg-rockchip-hwaccel.md) |
| 7 | GNOME Remote Desktop backend | Public `release/50.2-rkmpp@c4ef3c9` rebases all 16 existing release changes patch-identically onto latest GNOME 50 stable `18cc5f7` and adds a seventeenth narrow reconnect-timeout repair. Review of June `a3a1a32` rejected its globally single-use routing token and broad greeter-preservation paths; only a display explicitly reassigned into the persistent user handover may now survive timeout, retaining its `RemoteId` listener so the next authenticated attempt can retry without restarting the daemon. The `~rk1` arm64 build `33452991` failed without retained diagnostics; a local rebuild reproduced the boundary as a successful RDP assertion followed by 21.64 s teardown against Meson's 20 s limit. Packaging-only `~rk2` changes no production source, keeps the test fatal with a 3× timeout multiplier, and is Published — source `18654077`, binary `247717203`, build `33461880` successful, with RDP green, zero failures, two expected skips. No defect in `50.2+rkmpp` is implicated in the earlier production oops: that was root-caused to the kernel's `CONFIG_DMABUF_DEBUG` (track 1), and two logins on a kernel without the option completed end to end at the reproduction geometry. Installation, measured idle-reconnect replay, sustained focus/resume, audio interoperability, and upstream review remain. | 2026-08-02 | [reconnect audit and fix](../findings/2026-07-29-rdp-reconnect-handover-redirect-race-and-inhibitor-idletime-reset.md), [color fix](../findings/2026-07-29-grd-fullrange-bt709-fixes-muted-colors.md), [kernel root cause](../findings/2026-07-28-dmabuf-debug-mangle-sg-table-is-the-sg-writer.md), [`apps/gnome-remote-desktop/docs/testing.md`](../apps/gnome-remote-desktop/docs/testing.md), [release patches](../apps/gnome-remote-desktop/patches/README.md), [audio diagnosis](../apps/gnome-remote-desktop/docs/audio-redirection.md), [ACK wedge](../findings/2026-07-20-grd-rdpgfx-focus-resume-ack-wedge.md) |
| 8 | Mesa / Panfrost | The upstream shape remains the open four-MR `gl_FragCoord` transfer stack: !42563 shader-image unbind fix, !42679 shared `u_blitter` fragcoord fix, !42613 Panfrost opt-in plus BLIT enablement, and !42614 the Gallium test setup/case. Tips and pipelines are unchanged; the corrected selected G610 reruns passed, while GitLab marks !42679 `need_rebase`. Separate interpolation probes show the remaining Mali-G610 varying drift is independent of `u_blitter` — the transfer fix routes around it but does not repair interpolation itself. | 2026-07-11 | [`video-libraries/mesa/README.md`](../video-libraries/mesa/README.md), [`blit-precision.md`](../video-libraries/mesa/docs/blit-precision.md), [`mr-review-findings.md`](../video-libraries/mesa/docs/mr-review-findings.md) |
| 9 | Launchpad PPA | Forward-port kernel `6.18.41+rk3588av1fwport20260802-0ubuntu1~rk1` is Published as source `18654047` with three binaries and successful arm64 build `33461848`; its `20260801` predecessor is Published as `18652965` with build `33460058`. GRD replacement source `18654077`, binary `247717203`, and build `33461880` are Published/successful. The archive also publishes current MPP, librga, co-installable FFmpeg 6.1, codec-udev, preceding GRD sources, and bounded-backpressure FFmpeg `7:8.0.3+rockchip+git20260719.da5befc806-0ubuntu1~rk1`; dedicated rewrite sources and experimental GRD `~exp3` remain Published/successful. Every board-side gate is open: exact installation, combined runtime, clean migration, board validation, and kernel revert. | 2026-08-02 | [`packaging/ppa/README.md`](../packaging/ppa/README.md), [`packaging/ppa/kernel-forward-port/README.md`](../packaging/ppa/kernel-forward-port/README.md), [upload log](../packaging/ppa/history/2026-07-06-ubuntu-rock-5b-upload-log.md) |
| 10 | Binary publishing | Built binaries are intentionally absent from git. No GitHub Releases have been published yet; release assets should be tagged with the kernel `PHASH` or package version and include checksums when the first release is cut. This is gated on the repository-wide license decision, not on engineering work. | 2026-07-01 | [`packaging/README.md`](../packaging/README.md), [`LICENSE.md`](../LICENSE.md) |
| 11 | Kodi HW decode | Stock Kodi 22 can auto-select the fork's H.264/HEVC/AV1 RKMPP decoders through `AVCodecHWConfig` and DRM PRIME, so no Kodi decoder-selection patch is needed. The required MPP runtime and FFmpeg packages are public; the Kodi GBM/GLES build, tty1 playback test, and Kodi PPA package remain pending. | 2026-07-11 | [`apps/kodi/README.md`](../apps/kodi/README.md), [decoder selection](../findings/2026-07-11-kodi-ffmpeg-rockchip-hwaccel.md) |
| 12 | ROCK 5B SD/SPI boot chain | The working baseline boots Armbian from NVMe through SPI, and the tested SD rootfs boots when its raw-loader area is cleared so BootROM uses that SPI path. The four-way U-Boot comparison found a required zero-byte control DTB in both failing vendor artifacts, while the untested 26.5.1 `current` package contains a valid 12,752-byte DTB; the Radxa tree's missing build dependency makes a parallel-build race the leading code-supported explanation, not yet hardware proof. The decisive discriminator remains writing the captured 26.5.1 `current` raw loader to SD. | 2026-07-11 | [`boot-firmware/docs/version-comparison.md`](../boot-firmware/docs/version-comparison.md), [SD boot investigation](../findings/2026-07-09-rock5b-armbian-sd-boot-investigation.md) |
| 13 | Maximum-mainline kernel | The 2026-08-02 refresh pins Linux `7.2-rc6` at Torvalds `master@075b74841bd0`, public `e6951bc3f935` (299 commits), WIP `73d29539f7bb` (19 FRL-only commits), `next-20260731@415606a7be93`, public-next `0cae4ac66823` (264 commits), and WIP-next `15a5179dc3b2`. The 41-entry public ledger replaces six old revisions, adds public VP9/DCPHY/N/CTS, and records eight Linus-upstream dispositions; subsystem-next inspection found accepted VOP2, forced-color, USB-C retry, and RKVDEC H.265 work. Linus/public passes the full native `Image modules dtbs` build plus an incremental recheck; linux-next/WIP focused and partial-build checks pass, but its full build was stopped by request. The 2026-07-17 package/header results belong to superseded sources, so refreshed packages, installation, boot, platform, display, media, suspend, and rollback evidence are all open. | 2026-08-02 | [refresh audit](../findings/2026-08-02-rk3588-maxline-proposal-refresh.md), [`packaging/ppa/kernel-maxline/README.md`](../packaging/ppa/kernel-maxline/README.md), [`verification.md`](../packaging/ppa/kernel-maxline/verification.md), [`board-support.md`](../packaging/ppa/kernel-maxline/board-support.md) |
| 14 | Desktop-app HW video (browsers) | Installed driver/config `1.0.11+ysp8-0ubuntu1~rk1` are `dpkg -V` clean with the installed driver hash matching the deb exactly. Green: the guarded safe decode matrix, external-buffer/concurrent decode, 10-bit P010 decode above 60 fps, H.264/HEVC encode, imported surfaces, multi-slice, mixed decode/encode, and GStreamer VA readback. In a fresh Mutter/Panfrost virtual monitor, installed VLC 3.0.23, mpv 0.41.0, and Firefox 153.0.1 each present H.264, HEVC Main, VP9 Profile 0, HEVC Main10, and VP9 Profile 2; Firefox imports both 10-bit planes but ran with its RDD sandbox disabled. The one kernel warning pair is root-caused to libmpp's optional IEP2 deinterlace probe selecting generic `/dev/mpp_service` without an IEP2 subdevice, and the triggering interlaced clip stays bit-exact — RK3588 has IEP2 hardware and no VDPP instance. The 6.18 source port carries the IEP2 driver/DT path with compile-tested lifetime, fault, teardown, clock, DMA-boundary, and IOVA hardening, but KASAN package `Pcf86-Cc271` is unbooted and the installed kernel still omits client 28. The formerly suspect small-geometry dropped write is scoped to the rewrite driver only: the production forward-port/vendor RGA3 path passed 90/90 runs and 4,320/4,320 byte-compared frames on both RGA3 cores. Open: ysp8 was built from a dirty worktree over `main@aee5926`, CMA is 256 rather than 512 MiB, and sandbox-enabled Firefox, physical HDR, Chromium GL, clean-image install, and release remain. | 2026-08-02 | [`video-libraries/vaapi/README.md`](../video-libraries/vaapi/README.md), [installed ysp8 validation](../findings/2026-08-02-rockchip-vaapi-ysp8-installed-runtime-validation.md), [ysp9 RC validation](../findings/2026-08-02-rockchip-vaapi-ysp9-rc-validation.md), [forward-port RGA discriminator](../findings/2026-08-02-rga3-forward-port-small-geometry-discriminator.md), [IEP2 safety review](../kernel-drivers/iep2/docs/forward-port-safety-review.md), [`docs/app-enablement.md`](./app-enablement.md) |
| 15 | CPU voltage binning (PVTM/eFuse) | Design only; no patch, branch, or build exists. Mainline ships the BSP's unbinned worst-die CPU voltage column exactly, and booting the vendor BSP resolved what this die is entitled to: `pvtm-volt-sel=5/7/7`, each index reproducing from its cluster's `rockchip,pvtm-voltage-sel` bucket table, with two independent confirmations that the index is applied rather than merely computed. Entitlement is −37.5 to −87.5 mV on nine of the ten non-trivial CPU OPPs and ≈−20% dynamic power on the big clusters at 1800/2016 — but exactly 0 mV at 2400 MHz, so a `performance`-pinned big cluster gains nothing from the mechanism. Three scope corrections shrink the port (`rockchip_pvtm.c` is unused by RK3588 CPUs, `leakage=` does not select CPU voltage here, and `rk3588_change_length()` is unreachable at 5/7/7); a fourth changes its upstream shape, since `drivers/soc/mediatek/mtk-svs.c` gives live per-die measurement in-tree precedent, making this a `drivers/soc/rockchip/` process monitor rather than a cpufreq special case. Nothing has been applied to a mainline kernel, no power was measured, and the cold-boot, SRAM read-margin, and shared-DSU-rail risks are all open. | 2026-07-27 | [`kernel-versions/docs/pvtm-opp-binning-plan.md`](../kernel-versions/docs/pvtm-opp-binning-plan.md), [measured index](../findings/2026-07-27-rk3588-pvtm-volt-sel-measured.md), [BSP/mainline comparison](../findings/2026-07-25-rk3588-cpu-voltage-binning-bsp-vs-mainline.md) |

## Promotion rule

When a row becomes stable enough to be project-owned, move the detail to the
owning project doc and leave only a link here. When a fact can change without a
repo edit, keep the maintenance trigger in [`../status.md`](../status.md)'s
watchlist rather than only recording it here.

A row records the state, not the road to it. When new evidence supersedes a
row, rewrite the row and let the dated finding carry what changed — do not
append a correction paragraph below the table, which leaves the table itself
saying something the repo no longer believes.
