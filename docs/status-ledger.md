# Status ledger

This is the audit companion to [`../status.md`](../status.md). Keep
`status.md` as the status dashboard and watchlist; use this file for
longer dated notes that cross project boundaries but are not the canonical home
for the underlying evidence.

Canonical detail still belongs in the owning project docs linked from each row.

| # | Track | Longer dated note | Last verified | Canonical detail |
|---|-------|-------------------|---------------|------------------|
| 1 | Kernel forward-port | Combined `=y` kernel build `Pb6ab-Cb831` is the primary validated path: H.264/H.265 encode, H.264/H.265 decode, RGA, and full hardware transcode were re-run on hardware. The `P1c9d` av1-fwport superset build also exposes AV1 through `/dev/mpp_service` and validates H.264/H.265/VP9/AV1 decode bit-exact against software. Caveat: the board's prebuilt `/usr` MPP cannot drive the decoders because required parser registration is missing; use a from-source MPP runtime for those paths. | 2026-07-04 | [`kernel-drivers/docs/forward-port-status.md`](../kernel-drivers/docs/forward-port-status.md), [`kernel-drivers/tests/README.md`](../kernel-drivers/tests/README.md), [`kernel-drivers/av1/docs/av1-rk3588.md`](../kernel-drivers/av1/docs/av1-rk3588.md) |
| 2 | BSP-audit fix series | The 65-patch split series applies to the forward-port tip, but is not shippable. The initial split matched the adversarially-verified aggregate; the later regeneration intentionally diverges in 8 files and is not covered by that verification. The current series also does not compile until patch 0024 is regenerated around `rkvenc2_free_rcbbuf()`. Runtime codec regression testing has not been run on a kernel carrying the fixes. | 2026-07-01 | [`kernel-drivers/patches/cleanup-split/README.md`](../kernel-drivers/patches/cleanup-split/README.md), [`kernel-drivers/docs/bsp-audit.md`](../kernel-drivers/docs/bsp-audit.md) |
| 3 | DKMS channel | The stock-kernel DKMS package compiles and links on the documented 6.18 target. The DT overlay is `dtc`-validated but not boot-validated, and the channel is mutually exclusive with the combined built-in kernel. | 2026-07-01 | [`packaging/dkms/README.md`](../packaging/dkms/README.md) |
| 4 | Clean-room rewrite drivers | The package heads are now composites: 6.18.38 `8daf5e9513b8` layers the clean-room rewrite after the exact Armbian current/forward-port snapshot, and v7.2-rc3 `24f7424fb958` layers it after Armbian bleedingedge patch identity `7.2-rc3-Sa13c-D398d-P86f0-Cd50f-H8200-HK01ba-Vc222-B5272-R448a`. On 2026-07-16 both clean archives built the Rockchip IOMMU provider, KUnit-enabled rewrite objects, and ROCK 5B DTB warning-free, and both Debian source plus full native arm64 binary builds completed. Dedicated PPAs accepted replacement sources `18623665` and `18623666`; arm64 builds `33406491` and `33406492` were running at 12:37 PDT while the historical 6.18.0 and 7.2-rc2 binaries remained Published. Device-free conformance validation has passed, but there is no booted rewrite hardware-validation record yet. | 2026-07-16 | [`kernel-drivers/docs/rewrite-drivers.md`](../kernel-drivers/docs/rewrite-drivers.md), [`kernel-drivers/tests/rewrite-conformance.md`](../kernel-drivers/tests/rewrite-conformance.md), [`packaging/ppa/README.md`](../packaging/ppa/README.md) |
| 5 | FFmpeg tree | Three canonical branches are published in `yisding/ffmpeg-rockchip-81`: `main@8b57e531d1fc` follows `FFmpeg/master@ceabc9b306f5`, `ffmpeg-80@be753f3bbb2c` follows `release/8.0@435ae0581deb`, and `ffmpeg-81@8d3ca020b6a2` follows `release/8.1@94138f6973dd`. Each carries the complete reworked nyanmisaka patchset plus all unique refactor/Jellyfin correctness work; the former PR #1 lineage is integrated. All three configured and compiled through the affected Rockchip, registration, V4L2, filter, swscale, demux/mux, and metadata objects, and `fate-source` passed. This is source validation, not new RK3588 runtime proof. The broad package/encode/RGA smoke point remains historical `refactor/section-c@75638e7f0b17`; the dedicated PPA still exports `be367abfe6`, and the normal system PPA still exports the older 8.0 port. Board re-validation of AV1 from MP4/MKV remains pending. | 2026-07-16 | [`video-libraries/ffmpeg/README.md`](../video-libraries/ffmpeg/README.md), [`video-libraries/ffmpeg/docs/rebase-notes.md`](../video-libraries/ffmpeg/docs/rebase-notes.md), [`findings/2026-07-11-kodi-ffmpeg-rockchip-hwaccel.md`](../findings/2026-07-11-kodi-ffmpeg-rockchip-hwaccel.md) |
| 6 | FFmpeg submissions | Nothing has been submitted upstream yet. The targeting analysis exists and separates upstream-FFmpeg candidates from nyanmisaka-fork patches, including the first-wave order. | 2026-07-02 | [`video-libraries/ffmpeg/docs/submission-plan.md`](../video-libraries/ffmpeg/docs/submission-plan.md) |
| 7 | GNOME Remote Desktop backend | The complete 13-patch code series and its 8-patch backend subset replay on `c14e09e` (`50.1` + 16); pristine tag `50.1` lacks the `cf250ed` context required by patch `0003`. The measured hardware path sustains 60 fps with 1.26 ms median MPP encode time, and backpressure fallback/recovery were hardware-verified in a dummy session. Corrected branch `rdp-handover-reconnect-v2@eb91daf` restores GNOME 50's two-stage handover, fixes variant/socket/timer ownership, and coalesces only concurrently pending redirects. A live `~exp1` handover session later froze after Firefox opened without a corresponding kernel fault. Diagnostic branch `debug/exp1-frame-starvation@1c870bc` adds rate-limited buffer/view/stale-drop/encode/submit/refresh/reset/cooldown counters and a suspected-starvation warning without changing scheduling. Full/local-package builds and the RDP test pass; Firefox-freeze capture, the original macOS reconnect scenario, and upstream MR remain pending. | 2026-07-17 | [`apps/gnome-remote-desktop/docs/profiling.md`](../apps/gnome-remote-desktop/docs/profiling.md), [`apps/gnome-remote-desktop/patches/README.md`](../apps/gnome-remote-desktop/patches/README.md) |
| 8 | Mesa / Panfrost | The upstream shape remains the open four-MR `gl_FragCoord` transfer stack: !42563 shader-image unbind fix, !42679 shared `u_blitter` fragcoord fix, !42613 Panfrost opt-in plus BLIT enablement, and !42614 the Gallium test setup/case. Tips and pipelines are unchanged; the corrected selected G610 reruns passed, while GitLab now marks !42679 `need_rebase`. Separate interpolation probes show the remaining Mali-G610 varying drift is independent of `u_blitter`; the transfer fix routes around it but does not repair interpolation itself. | 2026-07-11 | [`video-libraries/mesa/README.md`](../video-libraries/mesa/README.md), [`video-libraries/mesa/docs/blit-precision.md`](../video-libraries/mesa/docs/blit-precision.md), [`video-libraries/mesa/docs/mr-review-findings.md`](../video-libraries/mesa/docs/mr-review-findings.md) |
| 9 | Launchpad PPA | The recreated arm64-only main PPA has seven Published source/binary sets: MPP, librga, co-installable FFmpeg 6.1, the prior forward-port kernel, honest FFmpeg 8.0.3, GRD `~rk2`, and codec-udev 1.1. It also accepted the current forward-port kernel source; the incompatible FFmpeg/rewrite-kernel tracks remain isolated in dedicated archives. The experimental archive accepted diagnostic GRD source `18625943` (`50.1+rkmpp+git20260717.1c870bc-0ubuntu1~exp2`); arm64 build `33411510` succeeded in 6m24s and awaits publication. Reconnect-v2 `~exp1` build `33399816` had succeeded previously. The experimental archive is not a normal install target. Kernel/package publication, optional GDM ACL upload, GRD Firefox-freeze/reconnect runtime tests and promotion, exact clean-migration transaction, and kernel board install/revert gates remain pending. | 2026-07-17 | [`packaging/ppa/README.md`](../packaging/ppa/README.md), [`packaging/ppa/kernel-forward-port/README.md`](../packaging/ppa/kernel-forward-port/README.md), [`packaging/ppa/2026-07-06-ubuntu-rock-5b-upload-log.md`](../packaging/ppa/2026-07-06-ubuntu-rock-5b-upload-log.md) |
| 10 | Binary publishing | Built binaries are intentionally absent from git. No GitHub Releases have been published yet; release assets should be tagged with the kernel `PHASH` or package version and include checksums when the first release is cut. | 2026-07-01 | [`packaging/README.md`](../packaging/README.md) |
| 11 | Kodi HW decode | Stock Kodi 22 can auto-select the fork's H.264/HEVC/AV1 RKMPP decoders through `AVCodecHWConfig` and DRM PRIME, so no Kodi decoder-selection patch is needed. The required MPP runtime and FFmpeg packages are public; the Kodi GBM/GLES build, tty1 playback test, and Kodi PPA package remain pending. | 2026-07-11 | [`apps/kodi/README.md`](../apps/kodi/README.md), [`findings/2026-07-11-kodi-ffmpeg-rockchip-hwaccel.md`](../findings/2026-07-11-kodi-ffmpeg-rockchip-hwaccel.md) |
| 12 | ROCK 5B SD/SPI boot chain | The working baseline boots Armbian from NVMe through SPI, and the tested SD rootfs boots when its raw-loader area is cleared so BootROM uses that SPI path. The four-way U-Boot comparison found a required zero-byte control DTB in both failing vendor artifacts, while the untested 26.5.1 `current` package contains a valid 12,752-byte DTB; the Radxa tree's missing build dependency makes a parallel-build race the leading code-supported explanation, not yet hardware proof. The decisive discriminator remains writing the captured 26.5.1 `current` raw loader to SD. | 2026-07-11 | [`boot-firmware/docs/version-comparison.md`](../boot-firmware/docs/version-comparison.md), [`findings/2026-07-09-rock5b-armbian-sd-boot-investigation.md`](../findings/2026-07-09-rock5b-armbian-sd-boot-investigation.md) |

## Promotion rule

When a row becomes stable enough to be project-owned, move the detail to the
owning project doc and leave only a link here. When a fact can change without a
repo edit, keep the maintenance trigger in [`../status.md`](../status.md)'s
watchlist rather than only recording it here.
