# Status ledger

This is the audit companion to [`../status.md`](../status.md). Keep
`status.md` as the status dashboard and watchlist; use this file for the
cross-project note behind each track — what the current state rests on, and
which boundary is still open.

Each row states **current** state only. The superseded chain — every earlier
explanation, incident, and retracted premise — belongs to the dated findings
linked in the last column, which own the experiment and its trust tags. Do not
re-narrate that history here; a row that grows into a chronology has stopped
being an audit companion. Canonical mechanism still belongs in the owning
project docs.

| # | Track | Longer dated note | Last verified | Canonical detail |
|---|-------|-------------------|---------------|------------------|
| 1 | Kernel forward-port | Maintained `rk3588-video-6.18`, exported series, Published binaries, and installed image/DTB/headers are aligned at `7d53bc7a3adc` / `0001`–`0092` / `6.18.42+rk3588av1fwport20260804-0ubuntu1~rk1`. The three-patch tail closes the RGA job-task UAF, provider fault-callback/current-task lifetime, soft-CCU retire-before-reset ordering, and lost concurrent reset requests; all six affected arm64 objects pass `W=1`. The exact artifact boots as `6.18.42-ysp-rockchip64` with taint `0` and passes ABI, 12/12 MPP, 21/21 required plus 3/3 diagnostic FFmpeg, direct librga/RGA, decoder liveness/quality, IOMMU machinery, VP9/RGA recovery, broad independent VA-API, bounded fatal-log, and the flat two-hour dual-codec encode soak. GStreamer is 100/102 with two classified userspace semantics failures and no kernel fault. The two-hour 4K decode workload and kernel scan pass, but its strict resource oracle rejects a 36-fd transient span against the 32-fd limit despite head/tail medians falling 56→54. This is a green kernel functional/recovery verdict with a non-green userspace soak oracle, not KASAN/lockdep proof; root-only debugfs counters, authenticated RDP/display integration, and targeted hostile paths remain. The documented SD rescue + `kernel-revert.sh` commands are operator-validated. Two resolved premises still shape the series: the GRD first-frame scatterlist oops was `CONFIG_DMABUF_DEBUG`'s `mangle_sg_table()`, not driver corruption; and the `…20260725` orig was rewrite-tree contaminated, so the exporter excludes `drivers/video/rockchip/*-rewrite/`. | 2026-08-04 | [production validation](../findings/2026-08-04-forward-port-6-18-42-0092-production-validation.md), [0092 fix finding](../findings/2026-08-04-forward-port-rga-uaf-recovery-safety-fixes.md), [`packaging/ppa/kernel-forward-port/README.md`](../packaging/ppa/kernel-forward-port/README.md), [`kernel-drivers/docs/forward-port-status.md`](../kernel-drivers/docs/forward-port-status.md), [recovery finding](../findings/2026-08-04-forward-port-sd-rescue-rollback-used.md), [dma-buf root cause](../findings/2026-07-28-dmabuf-debug-mangle-sg-table-is-the-sg-writer.md) |
| 2 | BSP-audit fixes | The audit's 16 HIGH reviewer rows collapse to 13 distinct bugs: two were already absent from the later forward-port and the remaining 11 were semantically ported as `0058`–`0068`. Booted KASAN/lockdep package `Pabd5-C4ad2` passed the broad codec/ABI suites and the destructive foreign-fd, physical-import, reset-session, clientless-release, and cross-session paths; later `0071`-tip root gates and the Published `…20260723~rk1` production kernel's full conformance/root run were also green. That evidence validates the integrated tail, **not** every HIGH fix in isolation: acquire-fence cleanup, queued-job shutdown, missing-plane rejection, and partial-handle unwind still need targeted hostile-path gates. The historical 65-patch MEDIUM/LOW cleanup series remains unshippable — its strengthened form diverges from the verified draft and patch `0024` lacks a forward declaration. | 2026-07-24 | [port record](../findings/2026-07-22-bsp-high-current-tip-port.md), [production run](../findings/2026-07-24-production-ppa-kernel-full-conformance-run.md), [`kernel-drivers/docs/bsp-audit.md`](../kernel-drivers/docs/bsp-audit.md), [`cleanup-split`](../kernel-drivers/patches/cleanup-split/README.md) |
| 3 | DKMS channel | The stock-kernel DKMS package compiles and links on the documented 6.18 target. The DT overlay is `dtc`-validated but not boot-validated, and the channel is mutually exclusive with the combined built-in kernel. | 2026-07-01 | [`packaging/dkms/README.md`](../packaging/dkms/README.md) |
| 4 | Clean-room rewrite drivers | Maintained tips are 6.18 `37ae7459656b575bdaab5ce19d01f1d78b41228b` on `v6.18.42` and mainline `02bf372dac70add49b71aca63811c7742b795786` on `v7.2-rc6`. The tracked MPP/RGA sources, Kconfig, ABI ledgers, and UAPI remain byte-identical, and the exact 92 MPP + 152 RGA manifest is unchanged. Both tips pass the warning-fatal clean-archive `normal` build, including Rockchip/VSI IOMMU, KUnit-enabled rewrite objects, and the ROCK 5B DTB; the audit reports 305 known signals, zero new, and zero absent. The exact-tip 6.18 KASAN image/DTB/headers/libc-dev packages built successfully as `P27bb-Cad24` from 385 patches over `v6.18.42`; the image embeds `g37ae7459656b`, packaged config SHA-256 is `8e8fa957ae7e4fa5776116ccf849c7c85486e7cc48ec1fc6ca8b7c8e49aa88b4`, and the required KASAN/lockdep/rewrite/KUnit symbols are enabled. The package is not installed or booted. Predecessor KASAN boot `#2 g19634f4eebba` is the first exact current-manifest hardware result: 92/92 MPP and 152/152 RGA, zero failures/skips, clean outer interval, and live lockdep. The official MPP suite passes 12/12. Its librga run proves the earlier imported-handle repair, then exposes a USERPTR scatterlist coalescing bug that exceeds SWIOTLB's 256-KiB per-mapping limit and a legacy zero-fence sentinel mismatch; it also shows a successful RGA2 multi-SG fallback logged as rejected. The fixed tips cap SG entries with the DMA backend limit, normalize fd zero only for legacy BLIT, delay that diagnostic, and repair the suite's `IM_STATUS_SUCCESS == 1` classification. Those changes now have exact-tip package proof but remain unbooted. Test-disabled/memory/race, aged kmemleak, corrected full librga, AV1/VSI, recovery, performance, fuzz, and soak remain open; the proposed ownership objects are still design only. | 2026-08-05 | [librga root cause, fix, and package build](../findings/2026-08-05-rewrite-rga-librga-swiotlb-fence-status.md), [current rebase/build finding](../findings/2026-08-04-rewrite-kernel-rebase-6-18-42-7-2-rc6.md), [architecture guide](../kernel-drivers/docs/rewrite-driver-architecture/README.md), [KUnit contract](../kernel-drivers/docs/rewrite-kunit.md), [AV1 assessment](../kernel-drivers/av1/docs/av1-rewrite-assessment.md) |
| 5 | ffmpeg tree | Canonical `main@8b57e531d1fc` retains source/FATE-only proof, but as of the 2026-08-04 [W07 recheck](../status.md#watch-w07) `ffmpeg-80` and `ffmpeg-81` have both moved (to `ab675f19cf17` and `629f4968d226`), so **their source/FATE evidence does not carry to the current heads** and both need a fresh replay. Broad package/encode/RGA smoke remains historical `75638e7f0b17`. The normal-system package branch advances to `fix/rkmpp-output-timeout@c9428bedaa`, which fixes the asynchronous `MppFrame` reset/close double release: nonblocking MPP owns a submitted frame until packet return or teardown, while the old close path destroyed MPP and then deinitialized the same stored pointer. Affected-object and `fate-source` builds, source construction/checksum verification, and focused RK3588 gates pass — 10/10 immediate close and 10/10 flush/reuse with no refcount, frame-pool, or kernel-fault line. Its `.dsc`, source `.buildinfo`, and `.changes` have good direct GPG signatures; `dput` passed every client-side gate and transferred the source set on 2026-08-05. Launchpad acceptance/build/publication were not awaited or rechecked. Current `nyanmisaka/ffmpeg-rockchip@388741a3544b` shares the defect; mainline FFmpeg `a441a2eb3839` uses a distinct blocking implementation and does not. The last-confirmed Published/installed packages remain at predecessor `33a651a55b`; candidate installation, the real GRD fallback/recreation gate, canonical-tip hardware, and AV1 MP4/MKV validation are open. | 2026-08-05 | [lifetime fix](../findings/2026-07-30-ffmpeg-rkmpp-async-frame-lifetime-fix.md), [`video-libraries/ffmpeg/README.md`](../video-libraries/ffmpeg/README.md), [input backpressure](../findings/2026-07-19-grd-rkmpp-encoder-wedge-mpp-input-backpressure.md), [AV1 follow-up](../findings/2026-07-11-kodi-ffmpeg-rockchip-hwaccel.md) |
| 7 | GNOME Remote Desktop backend | Public `release/50.2-rkmpp@c4ef3c9` rebases all 16 existing release changes patch-identically onto latest GNOME 50 stable `18cc5f7` and adds a seventeenth narrow reconnect-timeout repair. Review of June `a3a1a32` rejected its globally single-use routing token and broad greeter-preservation paths; only a display explicitly reassigned into the persistent user handover may now survive timeout, retaining its `RemoteId` listener so the next authenticated attempt can retry without restarting the daemon. The `~rk1` arm64 build `33452991` failed without retained diagnostics; a local rebuild reproduced the boundary as a successful RDP assertion followed by 21.64 s teardown against Meson's 20 s limit. Packaging-only `~rk2` changes no production source, keeps the test fatal with a 3× timeout multiplier, and is Published — source `18654077`, binary `247717203`, build `33461880` successful, with RDP green, zero failures, two expected skips. No defect in `50.2+rkmpp` is implicated in the earlier production oops: that was root-caused to the kernel's `CONFIG_DMABUF_DEBUG` (track 1), and two logins on a kernel without the option completed end to end at the reproduction geometry. Installation, measured idle-reconnect replay, sustained focus/resume, audio interoperability, and upstream review remain. | 2026-08-02 | [reconnect audit and fix](../findings/2026-07-29-rdp-reconnect-handover-redirect-race-and-inhibitor-idletime-reset.md), [color fix](../findings/2026-07-29-grd-fullrange-bt709-fixes-muted-colors.md), [kernel root cause](../findings/2026-07-28-dmabuf-debug-mangle-sg-table-is-the-sg-writer.md), [`apps/gnome-remote-desktop/docs/testing.md`](../apps/gnome-remote-desktop/docs/testing.md), [release patches](../apps/gnome-remote-desktop/patches/README.md), [audio diagnosis](../apps/gnome-remote-desktop/docs/audio-redirection.md), [ACK wedge](../findings/2026-07-20-grd-rdpgfx-focus-resume-ack-wedge.md) |
| 8 | Mesa / Panfrost | The upstream shape remains the open four-MR `gl_FragCoord` transfer stack: !42563 shader-image unbind fix, !42679 shared `u_blitter` fragcoord fix, !42613 Panfrost opt-in plus BLIT enablement, and !42614 the Gallium test setup/case. As of 2026-08-04 all four are still open and unmerged, but the rebase requirement has moved: !42679 was updated 2026-07-23, reports no conflicts, and no longer needs one, while GitLab now marks **!42614** `need_rebase`. !42613 and !42614 still carry the exact heads (`8875a22856da`, `4c23f1db1f9c`) whose four selected G610 shards passed, so that evidence survives; !42679's green pipeline predates its current head and needs a rerun. Note that the other three report `unchecked`, which is GitLab declining to recompute mergeability rather than a positive verdict. Separate interpolation probes show the remaining Mali-G610 varying drift is independent of `u_blitter` — the transfer fix routes around it but does not repair interpolation itself. | 2026-08-04 | [`video-libraries/mesa/README.md`](../video-libraries/mesa/README.md), [`blit-precision.md`](../video-libraries/mesa/docs/blit-precision.md), [`mr-review-findings.md`](../video-libraries/mesa/docs/mr-review-findings.md) |
| 9 | Launchpad PPA | The last API recheck found all nine normal-PPA sources Published. The forward-port kernel publication milestone is closed: `6.18.42+rk3588av1fwport20260804-0ubuntu1~rk1` / `0092` source `18656958`, successful arm64 build `33467257`, and all three binaries are live, installed, previously booted, and broadly production-validated. MPP `1.5.0+git20260805.a8b19653+ds-0ubuntu1~rk1` source `18657949` is Published, build `33468629` succeeded, and the live-index packages are installed. Installed-package replay on rewrite KASAN boot `g19634f4eebba` passes the retained 16-frame VP9 oracle through all three decoder clients, 120/120 teardown stress, four 30-frame bit-exact codec differentials, and the 12/12 official suite with clean bounded kernel-journal scans. VA-API ysp13 source `18657954`, successful build `33468630`, and driver/config binaries are live; the same-version local build is installed and validated, while exact PPA-binary replay remains track 14's gate. After that recheck, the FFmpeg `c9428bedaa` source upload completed client-side; its Launchpad state was deliberately not queried. GRD `~rk2` remains Published/installed. Clean-install is a separate incompatible-stack migration exercise. The SD rescue + `kernel-revert.sh` commands are operator-validated. | 2026-08-05 | [`packaging/ppa/README.md`](../packaging/ppa/README.md), [MPP package closure](../findings/2026-08-04-libmpp-vp9-show-existing-reference-slot-leak.md#installed-ppa-package-closure), [`packaging/ppa/kernel-forward-port/README.md`](../packaging/ppa/kernel-forward-port/README.md), [kernel production validation](../findings/2026-08-04-forward-port-6-18-42-0092-production-validation.md), [recovery finding](../findings/2026-08-04-forward-port-sd-rescue-rollback-used.md), [upload log](../packaging/ppa/history/2026-07-06-ubuntu-rock-5b-upload-log.md) |
| 10 | Binary publishing | Built binaries remain intentionally absent from git, and the 2026-08-04 `gh release list` check found no GitHub Release. The repository-owned license gate closed on 2026-08-05: Yi Ding's documentation/non-code contributions are CC BY-SA 4.0; Yi Ding's code contributions follow the target upstream license; only Yi Ding's own original kernel contributions are GPL-2.0-or-later. That grant does not apply to upstream kernel code, imported patch context, or another author's contribution. Publication now depends on selecting exact artifacts, satisfying their corresponding-source and third-party redistribution obligations, and producing a versioned checksummed manifest. | 2026-08-05 | [`packaging/README.md`](../packaging/README.md), [`LICENSE.md`](../LICENSE.md) |
| 11 | Kodi HW decode | Stock Kodi 22 can auto-select the fork's H.264/HEVC/AV1 RKMPP decoders through `AVCodecHWConfig` and DRM PRIME, so no Kodi decoder-selection patch is needed. The required MPP runtime and FFmpeg packages are public; the Kodi GBM/GLES build, tty1 playback test, and Kodi PPA package remain pending. Re-checked 2026-08-04: the PPA still publishes no Kodi source, but the build and playback halves are dev-box/board state and stay unverified from the repository. | 2026-08-04 | [`apps/kodi/README.md`](../apps/kodi/README.md), [decoder selection](../findings/2026-07-11-kodi-ffmpeg-rockchip-hwaccel.md) |
| 12 | ROCK 5B SD/SPI boot chain | The working baseline boots Armbian from NVMe through SPI, and the tested SD rootfs boots when its raw-loader area is cleared so BootROM uses that SPI path. The four-way U-Boot comparison found a required zero-byte control DTB in both failing vendor artifacts, while the untested 26.5.1 `current` package contains a valid 12,752-byte DTB; the Radxa tree's missing build dependency makes a parallel-build race the leading code-supported explanation, not yet hardware proof. The decisive discriminator remains writing the captured 26.5.1 `current` raw loader to SD. | 2026-07-11 | [`boot-firmware/docs/version-comparison.md`](../boot-firmware/docs/version-comparison.md), [SD boot investigation](../findings/2026-07-09-rock5b-armbian-sd-boot-investigation.md) |
| 13 | Maximum-mainline kernel | The 2026-08-02 refresh pins Linux `7.2-rc6` at Torvalds `master@075b74841bd0`, public `e6951bc3f935` (299 commits), WIP `73d29539f7bb` (19 FRL-only commits), `next-20260731@415606a7be93`, public-next `0cae4ac66823` (264 commits), and WIP-next `15a5179dc3b2`. The 41-entry public ledger replaces six old revisions, adds public VP9/DCPHY/N/CTS, and records eight Linus-upstream dispositions; subsystem-next inspection found accepted VOP2, forced-color, USB-C retry, and RKVDEC H.265 work. Linus/public passes the full native `Image modules dtbs` build plus an incremental recheck; linux-next/WIP focused and partial-build checks pass, but its full build was stopped by request. The 2026-07-17 package/header results belong to superseded sources, so refreshed packages, installation, boot, platform, display, media, suspend, and rollback evidence are all open. | 2026-08-02 | [refresh audit](../findings/2026-08-02-rk3588-maxline-proposal-refresh.md), [`packaging/ppa/kernel-maxline/README.md`](../packaging/ppa/kernel-maxline/README.md), [`verification.md`](../packaging/ppa/kernel-maxline/verification.md), [`board-support.md`](../packaging/ppa/kernel-maxline/board-support.md) |
| 14 | Desktop-app HW video (browsers) | `1.0.11+ysp13-0ubuntu1~rk1` is now clean public release `main@70f26d9`, Published source `18657954`, successful arm64 build `33468630`, and live driver/config binaries. The same-version locally built package remains installed; its driver matches that deb payload at SHA-256 `ed578a241803f35f51ccc6afe38b1d132539ae48ca691b5a3180e37565fe56c0`. Its retained-export worker/lifecycle/sanitizer, 17-vector conformance, static-analysis and 1,440-frame zero-copy gates pass. Google Chrome 151 presents H.264 correctly instead of green and selects `VaapiVideoDecoder` for 640x480 VP9 Profile 0; 384x240 VP9 selects software by Chromium's below-360p priority. H.264, VP9 Profile 0 and HEVC Main remain default; Main10, VP9 Profile 2 and encode remain opt-in. Ysp12 retains the broad exact-kernel `0092` normal/sanitized decode/encode, HEVC-sweep and soak evidence; the Published ysp13 binary has not been installed and replayed. Chrome replay is manual; the stock Google deb uses no sandbox-disabling flags, but the live GPU process is unsandboxed. XtraDeb Chromium still omits libva. Open: exact PPA-binary install/replay, automated H.264/VP9/HEVC Chrome gates and sandbox, XtraDeb Chromium packaging, sandbox-enabled Firefox, physical HDR, clean-image install, 512 MiB CMA, the decode fd-span oracle, app/session selection, WebRTC introspection, and the FFmpeg-only same-process TSan race. | 2026-08-05 | [production validation](../findings/2026-08-04-forward-port-6-18-42-0092-production-validation.md), [`video-libraries/vaapi/README.md`](../video-libraries/vaapi/README.md), [Google Chrome installed replay](../findings/2026-08-04-google-chrome-rockchip-vaapi-green-stable-export.md), [Chromium 151 package boundary](../findings/2026-08-04-chromium-151-gpu-working-v4l2-only.md), [interlaced/IEP2 regression](../findings/2026-08-04-vaapi-interlaced-decode-broken-by-iep2-enablement.md) |
| 15 | CPU voltage binning (PVTM/eFuse) | Design only; no patch, branch, or build exists. Mainline ships the BSP's unbinned worst-die CPU voltage column exactly, and booting the vendor BSP resolved what this die is entitled to: `pvtm-volt-sel=5/7/7`, each index reproducing from its cluster's `rockchip,pvtm-voltage-sel` bucket table, with two independent confirmations that the index is applied rather than merely computed. Entitlement is −37.5 to −87.5 mV on nine of the ten non-trivial CPU OPPs and ≈−20% dynamic power on the big clusters at 1800/2016 — but exactly 0 mV at 2400 MHz, so a `performance`-pinned big cluster gains nothing from the mechanism. Three scope corrections shrink the port (`rockchip_pvtm.c` is unused by RK3588 CPUs, `leakage=` does not select CPU voltage here, and `rk3588_change_length()` is unreachable at 5/7/7); a fourth changes its upstream shape, since `drivers/soc/mediatek/mtk-svs.c` gives live per-die measurement in-tree precedent, making this a `drivers/soc/rockchip/` process monitor rather than a cpufreq special case. Nothing has been applied to a mainline kernel, no power was measured, and the cold-boot, SRAM read-margin, and shared-DSU-rail risks are all open. | 2026-07-27 | [`kernel-versions/docs/pvtm-opp-binning-plan.md`](../kernel-versions/docs/pvtm-opp-binning-plan.md), [measured index](../findings/2026-07-27-rk3588-pvtm-volt-sel-measured.md), [BSP/mainline comparison](../findings/2026-07-25-rk3588-cpu-voltage-binning-bsp-vs-mainline.md) |

## Promotion rule

When a row becomes stable enough to be project-owned, move the detail to the
owning project doc and leave only a link here. When a fact can change without a
repo edit, keep the maintenance trigger in [`../status.md`](../status.md)'s
watchlist rather than only recording it here.

A row records the state, not the road to it. When new evidence supersedes a
row, rewrite the row and let the dated finding carry what changed — do not
append a correction paragraph below the table, which leaves the table itself
saying something the repo no longer believes.
