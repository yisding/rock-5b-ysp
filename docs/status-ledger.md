# Status ledger

This is the audit companion to [`../status.md`](../status.md). Keep
`status.md` as the presentation scoreboard and watchlist; use this file for
longer dated notes that cross project boundaries but are not the canonical home
for the underlying evidence.

Canonical detail still belongs in the owning project docs linked from each row.

| # | Track | Longer dated note | Last verified | Canonical detail |
|---|-------|-------------------|---------------|------------------|
| 1 | Kernel forward-port | Combined `=y` kernel build `Pb6ab-Cb831` is the primary validated path: H.264/H.265 encode, H.264/H.265 decode, RGA, and full hardware transcode were re-run on hardware. The `P1c9d` av1-fwport superset build also exposes AV1 through `/dev/mpp_service` and validates H.264/H.265/VP9/AV1 decode bit-exact against software. Caveat: the board's prebuilt `/usr` MPP cannot drive the decoders because required parser registration is missing; use a from-source MPP runtime for those paths. | 2026-07-04 | [`kernel-drivers/docs/forward-port-status.md`](../kernel-drivers/docs/forward-port-status.md), [`kernel-drivers/tests/README.md`](../kernel-drivers/tests/README.md), [`kernel-drivers/av1/docs/av1-rk3588.md`](../kernel-drivers/av1/docs/av1-rk3588.md) |
| 2 | BSP-audit fix series | The 65-patch split series applies to the forward-port tip, but is not shippable. The initial split matched the adversarially-verified aggregate; the later regeneration intentionally diverges in 8 files and is not covered by that verification. The current series also does not compile until patch 0024 is regenerated around `rkvenc2_free_rcbbuf()`. Runtime codec regression testing has not been run on a kernel carrying the fixes. | 2026-07-01 | [`kernel-drivers/patches/cleanup-split/README.md`](../kernel-drivers/patches/cleanup-split/README.md), [`kernel-drivers/docs/bsp-audit.md`](../kernel-drivers/docs/bsp-audit.md) |
| 3 | DKMS channel | The stock-kernel DKMS package compiles and links on the documented 6.18 target. The DT overlay is `dtc`-validated but not boot-validated, and the channel is mutually exclusive with the combined built-in kernel. | 2026-07-01 | [`packaging/dkms/README.md`](../packaging/dkms/README.md) |
| 4 | Clean-room rewrite drivers | Current committed rewrite heads are `rk3588-rewrite-6.18` at `d1d15a3d052a` and `rk3588-rewrite-mainline` at `12f712d71144`, both including Rockchip IOMMU media fault-reporting hardening. These heads passed the default normal clean-archive build gate; broader `normal`/`memory`/`race` build coverage last passed at the immediately earlier `0a35c26a0fd7` and `938b1d2032c3` heads. Device-free conformance validation has passed, but there is no booted rewrite hardware-validation record yet. | 2026-07-06 | [`kernel-drivers/docs/rewrite-drivers.md`](../kernel-drivers/docs/rewrite-drivers.md), [`kernel-drivers/tests/rewrite-conformance.md`](../kernel-drivers/tests/rewrite-conformance.md) |
| 5 | FFmpeg tree | The local `ffmpeg-rockchip-81` package-validation tree is branch `refactor/section-c` at `75638e7f0b17`. It builds a self-contained package, registers RKMPP decoders/encoders and RKRGA filters, and passes H.264 RKMPP encode plus RKMPP hwupload to RGA to HEVC RKMPP encode smoke tests. Decode failure with the installed `/usr` MPP was traced to missing H.264 parser registration; a clean `mpp-rockchip` 1.0.12 build supplies the missing symbols and makes the same packaged FFmpeg binary decode. | 2026-07-06 | [`findings/2026-07-06-ffmpeg-rockchip81-package-validation.md`](../findings/2026-07-06-ffmpeg-rockchip81-package-validation.md), [`packaging/ffmpeg-rockchip81/README.md`](../packaging/ffmpeg-rockchip81/README.md), [`video-libraries/ffmpeg/docs/rebase-notes.md`](../video-libraries/ffmpeg/docs/rebase-notes.md) |
| 6 | FFmpeg submissions | Nothing has been submitted upstream yet. The targeting analysis exists and separates upstream-FFmpeg candidates from nyanmisaka-fork patches, including the first-wave order. | 2026-07-02 | [`video-libraries/ffmpeg/docs/submission-plan.md`](../video-libraries/ffmpeg/docs/submission-plan.md) |
| 7 | GNOME Remote Desktop backend | The 8-patch backend series applies to pristine GRD 50.1 and the measured hardware path sustains 60 fps with 1.26 ms median MPP encode time. Backpressure fallback and recovery were hardware-verified in a dummy session. The handover-reconnect fix remains parked on the fork branch `rdp-handover-reconnect`; no upstream MR exists yet. | 2026-07-03 | [`apps/gnome-remote-desktop/docs/profiling.md`](../apps/gnome-remote-desktop/docs/profiling.md), [`apps/gnome-remote-desktop/patches/README.md`](../apps/gnome-remote-desktop/patches/README.md) |
| 8 | Mesa / Panfrost | The upstream shape is the open four-MR `gl_FragCoord` transfer stack: !42563 shader-image unbind fix, !42679 shared `u_blitter` fragcoord fix, !42613 Panfrost opt-in plus BLIT enablement, and !42614 the Gallium test setup/case. The corrected selected G610 reruns passed. Separate interpolation probes show the remaining Mali-G610 varying drift is independent of `u_blitter`; the transfer fix routes around it but does not repair interpolation itself. | 2026-07-06 | [`video-libraries/mesa/README.md`](../video-libraries/mesa/README.md), [`video-libraries/mesa/docs/blit-precision.md`](../video-libraries/mesa/docs/blit-precision.md), [`video-libraries/mesa/docs/mr-review-findings.md`](../video-libraries/mesa/docs/mr-review-findings.md) |
| 9 | Launchpad PPA | Source packaging for `mpp`, `librga`, `ffmpeg`, and GRD is now in-repo with a reproducible export helper. At the `2026-07-06T16:43:41-07:00` public APT/API check, the public source index publishes MPP and librga `-0ubuntu2~rk1`, librga `-0ubuntu3~rk1` is still `Pending` in Launchpad API, public arm64 and amd64 binary indexes are empty, and no FFmpeg or GRD source is public in the APT index. The upstream FFmpeg baseline upload is `Pending` in Launchpad's API view, while the higher-version `ffmpeg-rockchip-81` and GRD uploads remained held. | 2026-07-06 | [`packaging/ppa/README.md`](../packaging/ppa/README.md), [`packaging/ppa/2026-07-06-ubuntu-rock-5b-upload-log.md`](../packaging/ppa/2026-07-06-ubuntu-rock-5b-upload-log.md) |
| 10 | Binary publishing | Built binaries are intentionally absent from git. No GitHub Releases have been published yet; release assets should be tagged with the kernel `PHASH` or package version and include checksums when the first release is cut. | 2026-07-01 | [`packaging/README.md`](../packaging/README.md) |

## Promotion rule

When a row becomes stable enough to be project-owned, move the detail to the
owning project doc and leave only a link here. When a fact can change without a
repo edit, keep the maintenance trigger in [`../status.md`](../status.md)'s
watchlist rather than only recording it here.
