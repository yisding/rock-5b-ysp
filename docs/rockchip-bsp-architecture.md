# Rockchip BSP architecture

The Rockchip BSP architecture documentation now lives in its own folder:
[`bsp/`](bsp/README.md).

Start with [`bsp/README.md`](bsp/README.md). The folder documents what the
Rockchip 6.1 BSP kernel adds compared with the stock 6.1 kernel it is based on,
with separate pages for each subsystem area.
