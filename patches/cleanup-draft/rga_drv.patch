commit 56e403ede0812f31741b173c89aa8cf93f7f2a6c
Author: rkvenc forward-port <forward-port@local>
Date:   Mon Jun 29 02:14:51 2026 -0700

    WIP: BSP audit cleanup edits (machine-generated, compile-tested)

diff --git a/drivers/video/rockchip/rga3/rga_drv.c b/drivers/video/rockchip/rga3/rga_drv.c
index 3ecd7ff6a325..1da91a3697f6 100644
--- a/drivers/video/rockchip/rga3/rga_drv.c
+++ b/drivers/video/rockchip/rga3/rga_drv.c
@@ -667,7 +667,7 @@ static long rga_ioctl_import_buffer(unsigned long arg, struct rga_session *sessi
 			       rga_get_memory_type_str(external_buffer[i].type),
 			       external_buffer[i].type);
 
-			goto err_free_external_buffer;
+			goto err_release_buffer;
 		}
 
 		external_buffer[i].handle = ret;
@@ -679,9 +679,12 @@ static long rga_ioctl_import_buffer(unsigned long arg, struct rga_session *sessi
 		rga_err("rga_buffer_pool external_buffer list copy_to_user failed\n");
 		ret = -EFAULT;
 
-		goto err_free_external_buffer;
+		goto err_release_buffer;
 	}
 
+err_release_buffer:
+	while (i-- > 0)
+		rga_mm_release_buffer(external_buffer[i].handle);
 err_free_external_buffer:
 	kfree(external_buffer);
 	return ret;
@@ -748,7 +751,7 @@ static long rga_ioctl_release_buffer(unsigned long arg)
 
 static long rga_ioctl_request_create(unsigned long arg, struct rga_session *session)
 {
-	uint32_t id;
+	int id;
 	uint32_t flags;
 
 	if (copy_from_user(&flags, (void *)arg, sizeof(uint32_t))) {
@@ -757,6 +760,10 @@ static long rga_ioctl_request_create(unsigned long arg, struct rga_session *sess
 	}
 
 	id = rga_request_alloc(flags, session);
+	if (id < 0) {
+		rga_err("%s request alloc failed!\n", __func__);
+		return id;
+	}
 
 	if (copy_to_user((void *)arg, &id, sizeof(uint32_t))) {
 		rga_err("%s failed to copy to user!\n", __func__);
@@ -801,7 +808,8 @@ static long rga_ioctl_request_submit(unsigned long arg, bool run_enbale)
 		ret = rga_request_submit(request);
 		if (ret < 0) {
 			rga_err("request[%d] submit failed!\n", user_request.id);
-			return -EFAULT;
+			ret = -EFAULT;
+			goto err_put_request;
 		}
 
 		if (request->sync_mode == RGA_BLIT_ASYNC) {
@@ -809,16 +817,19 @@ static long rga_ioctl_request_submit(unsigned long arg, bool run_enbale)
 			if (copy_to_user((struct rga_req *)arg,
 					 &user_request, sizeof(user_request))) {
 				rga_err("copy_to_user failed\n");
-				return -EFAULT;
+				ret = -EFAULT;
+				goto err_put_request;
 			}
 		}
 	}
 
+	ret = 0;
+err_put_request:
 	mutex_lock(&request_manager->lock);
 	rga_request_put(request);
 	mutex_unlock(&request_manager->lock);
 
-	return 0;
+	return ret;
 }
 
 static long rga_ioctl_request_cancel(unsigned long arg)
@@ -942,7 +953,7 @@ static long rga_ioctl(struct file *file, uint32_t cmd, unsigned long arg)
 	int major_version = 0, minor_version = 0;
 	char version[16] = { 0 };
 	struct rga_version_t driver_version;
-	struct rga_hw_versions_t hw_versions;
+	struct rga_hw_versions_t hw_versions = { 0 };
 	struct rga_drvdata_t *rga = rga_drvdata;
 	struct rga_session *session = file->private_data;
 
@@ -1318,7 +1329,7 @@ static int init_scheduler(struct rga_scheduler_t *scheduler,
 			scheduler->core = RGA3_SCHEDULER_CORE1;
 			break;
 		default:
-			pr_err("scheduler failed to match RGA2\n");
+			pr_err("scheduler failed to match RGA3\n");
 			return -EINVAL;
 		}
 
@@ -1424,7 +1435,7 @@ static int rga_drv_probe(struct platform_device *pdev)
 	device_init_wakeup(dev, true);
 	pm_runtime_enable(scheduler->dev);
 
-	ret = pm_runtime_get_sync(scheduler->dev);
+	ret = pm_runtime_resume_and_get(scheduler->dev);
 	if (ret < 0) {
 		dev_err(dev, "failed to get pm runtime, ret = %d\n", ret);
 		goto pm_disable;
@@ -1433,6 +1444,7 @@ static int rga_drv_probe(struct platform_device *pdev)
 	ret = clk_bulk_prepare_enable(scheduler->num_clks, scheduler->clks);
 	if (ret < 0) {
 		dev_err(dev, "failed to enable clk\n");
+		pm_runtime_put_sync(scheduler->dev);
 		goto pm_disable;
 	}
 #endif /* #ifndef RGA_DISABLE_PM */
