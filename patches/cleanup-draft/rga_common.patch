commit 56e403ede0812f31741b173c89aa8cf93f7f2a6c
Author: rkvenc forward-port <forward-port@local>
Date:   Mon Jun 29 02:14:51 2026 -0700

    WIP: BSP audit cleanup edits (machine-generated, compile-tested)

diff --git a/drivers/video/rockchip/rga3/rga_common.c b/drivers/video/rockchip/rga3/rga_common.c
index d94110769ac6..32afba98e5cc 100644
--- a/drivers/video/rockchip/rga3/rga_common.c
+++ b/drivers/video/rockchip/rga3/rga_common.c
@@ -304,9 +304,9 @@ const char *rga_get_format_name(uint32_t format)
 		return "UYVY420";
 
 	case RGA_FORMAT_YCbCr_420_SP_10B:
-		return "YCrCb420SP10B";
-	case RGA_FORMAT_YCrCb_420_SP_10B:
 		return "YCbCr420SP10B";
+	case RGA_FORMAT_YCrCb_420_SP_10B:
+		return "YCrCb420SP10B";
 	case RGA_FORMAT_YCbCr_422_SP_10B:
 		return "YCbCr422SP10B";
 	case RGA_FORMAT_YCrCb_422_SP_10B:
@@ -754,9 +754,12 @@ void rga_swap_pd_mode(struct rga_req *req_rga)
 int rga_image_size_cal(int w, int h, int format,
 		       int *yrgb_size, int *uv_size, int *v_size)
 {
-	int yrgb = 0;
-	int uv = 0;
-	int v = 0;
+	s64 yrgb = 0;
+	s64 uv = 0;
+	s64 v = 0;
+
+	if (w <= 0 || h <= 0)
+		return -EINVAL;
 
 	switch (format) {
 	case RGA_FORMAT_RGBA_8888:
@@ -767,11 +770,11 @@ int rga_image_size_cal(int w, int h, int format,
 	case RGA_FORMAT_XRGB_8888:
 	case RGA_FORMAT_ABGR_8888:
 	case RGA_FORMAT_XBGR_8888:
-		yrgb = w * h * 4;
+		yrgb = (s64)w * h * 4;
 		break;
 	case RGA_FORMAT_RGB_888:
 	case RGA_FORMAT_BGR_888:
-		yrgb = w * h * 3;
+		yrgb = (s64)w * h * 3;
 		break;
 	case RGA_FORMAT_RGB_565:
 	case RGA_FORMAT_RGBA_5551:
@@ -792,27 +795,27 @@ int rga_image_size_cal(int w, int h, int format,
 	case RGA_FORMAT_VYUY_420:
 	case RGA_FORMAT_YUYV_420:
 	case RGA_FORMAT_UYVY_420:
-		yrgb = w * h * 2;
+		yrgb = (s64)w * h * 2;
 		break;
 	/* YUV FORMAT */
 	case RGA_FORMAT_YCbCr_444_SP:
 	case RGA_FORMAT_YCrCb_444_SP:
-		yrgb = w * h;
-		uv = w * h;
-		v = w * h;
+		yrgb = (s64)w * h;
+		uv = (s64)w * h;
+		v = (s64)w * h;
 		break;
 	case RGA_FORMAT_YCbCr_422_SP:
 	case RGA_FORMAT_YCrCb_422_SP:
 	/* 10bit format stride is externally configured. */
 	case RGA_FORMAT_YCbCr_422_SP_10B:
 	case RGA_FORMAT_YCrCb_422_SP_10B:
-		yrgb = w * h;
-		uv = w * h;
+		yrgb = (s64)w * h;
+		uv = (s64)w * h;
 		break;
 	case RGA_FORMAT_YCbCr_422_P:
 	case RGA_FORMAT_YCrCb_422_P:
-		yrgb = w * h;
-		uv = (w * h) >> 1;
+		yrgb = (s64)w * h;
+		uv = ((s64)w * h) >> 1;
 		v = uv;
 		break;
 	case RGA_FORMAT_YCbCr_420_SP:
@@ -820,36 +823,39 @@ int rga_image_size_cal(int w, int h, int format,
 	/* 10bit format stride is externally configured. */
 	case RGA_FORMAT_YCbCr_420_SP_10B:
 	case RGA_FORMAT_YCrCb_420_SP_10B:
-		yrgb = w * h;
-		uv = (w * h) >> 1;
+		yrgb = (s64)w * h;
+		uv = ((s64)w * h) >> 1;
 		break;
 	case RGA_FORMAT_YCbCr_420_P:
 	case RGA_FORMAT_YCrCb_420_P:
-		yrgb = w * h;
-		uv = (w * h) >> 2;
+		yrgb = (s64)w * h;
+		uv = ((s64)w * h) >> 2;
 		v = uv;
 		break;
 	case RGA_FORMAT_BPP8:
 	case RGA_FORMAT_YCbCr_400:
 	case RGA_FORMAT_A8:
 	case RGA_FORMAT_Y8:
-		yrgb = w * h;
+		yrgb = (s64)w * h;
 		break;
 	case RGA_FORMAT_BPP4:
 	case RGA_FORMAT_Y4:
-		yrgb = (w * h) >> 1;
+		yrgb = ((s64)w * h) >> 1;
 		break;
 	case RGA_FORMAT_BPP2:
-		yrgb = (w * h) >> 2;
+		yrgb = ((s64)w * h) >> 2;
 		break;
 	case RGA_FORMAT_BPP1:
-		yrgb = (w * h) >> 3;
+		yrgb = ((s64)w * h) >> 3;
 		break;
 	default:
 		rga_err("Unsuport format [0x%x]\n", format);
 		return -EFAULT;
 	}
 
+	if (yrgb + uv + v > INT_MAX)
+		return -EINVAL;
+
 	if (yrgb_size != NULL)
 		*yrgb_size = yrgb;
 	if (uv_size != NULL)
